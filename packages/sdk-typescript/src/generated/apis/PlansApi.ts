/* tslint:disable */
/* eslint-disable */
/**
 * Toolpath Engine API
 * Public REST API for Toolpath engine operations.  **Early access.** Machining plans and toolpath calculation — creating, listing, and reading plans, calculating toolpaths, and reading toolpath results and machining time — are available now but still gaining functionality; planned additions include plan constraints and specifying material and stock, among others. Breaking changes still follow the API major version, so you can build against them today. These operations are grouped under the `Plans` and `Toolpaths` tags and marked `x-toolpath-experimental`.  **Units.** All dimensional values — in parts and in feature details — are in millimetres; all angles are in degrees. Each part response also states this in its `units` field.  **Browser use.** CORS is per API key: a request only receives an `Access-Control-Allow-Origin` header when its `Origin` is in the allowlist configured on the key used (set when creating or editing the key in the portal). A key with no allowed origins is server-to-server only. A Bearer API key placed in client-side JavaScript is readable by anyone — restrict its origins, prefer a key granted read access only (writes made with it are rejected with 403 `read_only_key`), or proxy requests through your own server.  **Access and billing.** An API key is granted read and write access separately in each area of the API — the core API (uploads, part display, holders, and jobs), DFM (part analysis, feature datasheets, and plans), and Quoting (toolpaths and machining time) — and that access can be edited on an existing key in the portal. Every operation states its area in its `x-toolpath-product` field (`core`, `dfm`, or `quoting`) and whether the call is billed in `x-toolpath-metered`. Reads are free but still belong to their product: reading a plan needs DFM access, and reading toolpaths needs Quoting. `GET` reads; every other method writes. A key with no access to the operation’s area is refused with 403 `product_mismatch`; one with read access used for a write is refused with 403 `read_only_key`.
 *
 * The version of the OpenAPI document: 1.12.1
 *
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */

import * as runtime from '../runtime.js'
import {
  type PlanListResponse,
  PlanListResponseFromJSON,
  PlanListResponseToJSON,
} from '../models/PlanListResponse.js'
import {
  type PlanResponse,
  PlanResponseFromJSON,
  PlanResponseToJSON,
} from '../models/PlanResponse.js'
import {
  type ProblemDetails,
  ProblemDetailsFromJSON,
  ProblemDetailsToJSON,
} from '../models/ProblemDetails.js'
import {
  type QueuePartJobResponse,
  QueuePartJobResponseFromJSON,
  QueuePartJobResponseToJSON,
} from '../models/QueuePartJobResponse.js'

export interface CreatePlanRequest {
  id: string
  idempotencyKey?: string
}

export interface GetPlanRequest {
  planId: string
}

export interface ListPlansRequest {
  id: string
  page?: number
  pageSize?: number
}

/**
 *
 */
export class PlansApi extends runtime.BaseAPI {
  /**
   * Creates request options for createPlan without sending the request
   */
  async createPlanRequestOpts(requestParameters: CreatePlanRequest): Promise<runtime.RequestOpts> {
    if (requestParameters['id'] == null) {
      throw new runtime.RequiredError(
        'id',
        'Required parameter "id" was null or undefined when calling createPlan().',
      )
    }

    const queryParameters: any = {}

    const headerParameters: runtime.HTTPHeaders = {}

    if (requestParameters['idempotencyKey'] != null) {
      headerParameters['Idempotency-Key'] = String(requestParameters['idempotencyKey'])
    }

    if (this.configuration && this.configuration.accessToken) {
      const token = this.configuration.accessToken
      const tokenString = await token('ApiKeyAuth', [])

      if (tokenString) {
        headerParameters['Authorization'] = `Bearer ${tokenString}`
      }
    }

    let urlPath = `/v1/parts/{id}/plans`
    urlPath = urlPath.replace('{id}', encodeURIComponent(String(requestParameters['id'])))

    return {
      path: urlPath,
      method: 'POST',
      headers: headerParameters,
      query: queryParameters,
    }
  }

  /**
   * Queues a machining plan for an analyzed part and returns a job. Track the job, then read the plan with **Get a plan** (`GET /plans/{planId}`) or **List plans** (`GET /parts/{id}/plans`).  **Early access.** Machining plans and toolpath calculation are available now but still gaining functionality — planned additions include plan constraints and specifying material and stock, among others. Breaking changes still follow the API major version, so you can build against them today; expect new capabilities to arrive as they mature.
   * Create a plan
   */
  async createPlanRaw(
    requestParameters: CreatePlanRequest,
    initOverrides?: RequestInit | runtime.InitOverrideFunction,
  ): Promise<runtime.ApiResponse<QueuePartJobResponse>> {
    const requestOptions = await this.createPlanRequestOpts(requestParameters)
    const response = await this.request(requestOptions, initOverrides)

    return new runtime.JSONApiResponse(response, (jsonValue) =>
      QueuePartJobResponseFromJSON(jsonValue),
    )
  }

  /**
   * Queues a machining plan for an analyzed part and returns a job. Track the job, then read the plan with **Get a plan** (`GET /plans/{planId}`) or **List plans** (`GET /parts/{id}/plans`).  **Early access.** Machining plans and toolpath calculation are available now but still gaining functionality — planned additions include plan constraints and specifying material and stock, among others. Breaking changes still follow the API major version, so you can build against them today; expect new capabilities to arrive as they mature.
   * Create a plan
   */
  async createPlan(
    requestParameters: CreatePlanRequest,
    initOverrides?: RequestInit | runtime.InitOverrideFunction,
  ): Promise<QueuePartJobResponse> {
    const response = await this.createPlanRaw(requestParameters, initOverrides)
    return await response.value()
  }

  /**
   * Creates request options for getPlan without sending the request
   */
  async getPlanRequestOpts(requestParameters: GetPlanRequest): Promise<runtime.RequestOpts> {
    if (requestParameters['planId'] == null) {
      throw new runtime.RequiredError(
        'planId',
        'Required parameter "planId" was null or undefined when calling getPlan().',
      )
    }

    const queryParameters: any = {}

    const headerParameters: runtime.HTTPHeaders = {}

    if (this.configuration && this.configuration.accessToken) {
      const token = this.configuration.accessToken
      const tokenString = await token('ApiKeyAuth', [])

      if (tokenString) {
        headerParameters['Authorization'] = `Bearer ${tokenString}`
      }
    }

    let urlPath = `/v1/plans/{planId}`
    urlPath = urlPath.replace('{planId}', encodeURIComponent(String(requestParameters['planId'])))

    return {
      path: urlPath,
      method: 'GET',
      headers: headerParameters,
      query: queryParameters,
    }
  }

  /**
   * Returns a plan with its setups and operations. Requires **Create a plan** (`POST /parts/{id}/plans`) to have run and its job to have succeeded.  **Early access.** Machining plans and toolpath calculation are available now but still gaining functionality — planned additions include plan constraints and specifying material and stock, among others. Breaking changes still follow the API major version, so you can build against them today; expect new capabilities to arrive as they mature.
   * Get a plan
   */
  async getPlanRaw(
    requestParameters: GetPlanRequest,
    initOverrides?: RequestInit | runtime.InitOverrideFunction,
  ): Promise<runtime.ApiResponse<PlanResponse>> {
    const requestOptions = await this.getPlanRequestOpts(requestParameters)
    const response = await this.request(requestOptions, initOverrides)

    return new runtime.JSONApiResponse(response, (jsonValue) => PlanResponseFromJSON(jsonValue))
  }

  /**
   * Returns a plan with its setups and operations. Requires **Create a plan** (`POST /parts/{id}/plans`) to have run and its job to have succeeded.  **Early access.** Machining plans and toolpath calculation are available now but still gaining functionality — planned additions include plan constraints and specifying material and stock, among others. Breaking changes still follow the API major version, so you can build against them today; expect new capabilities to arrive as they mature.
   * Get a plan
   */
  async getPlan(
    requestParameters: GetPlanRequest,
    initOverrides?: RequestInit | runtime.InitOverrideFunction,
  ): Promise<PlanResponse> {
    const response = await this.getPlanRaw(requestParameters, initOverrides)
    return await response.value()
  }

  /**
   * Creates request options for listPlans without sending the request
   */
  async listPlansRequestOpts(requestParameters: ListPlansRequest): Promise<runtime.RequestOpts> {
    if (requestParameters['id'] == null) {
      throw new runtime.RequiredError(
        'id',
        'Required parameter "id" was null or undefined when calling listPlans().',
      )
    }

    const queryParameters: any = {}

    if (requestParameters['page'] != null) {
      queryParameters['page'] = requestParameters['page']
    }

    if (requestParameters['pageSize'] != null) {
      queryParameters['pageSize'] = requestParameters['pageSize']
    }

    const headerParameters: runtime.HTTPHeaders = {}

    if (this.configuration && this.configuration.accessToken) {
      const token = this.configuration.accessToken
      const tokenString = await token('ApiKeyAuth', [])

      if (tokenString) {
        headerParameters['Authorization'] = `Bearer ${tokenString}`
      }
    }

    let urlPath = `/v1/parts/{id}/plans`
    urlPath = urlPath.replace('{id}', encodeURIComponent(String(requestParameters['id'])))

    return {
      path: urlPath,
      method: 'GET',
      headers: headerParameters,
      query: queryParameters,
    }
  }

  /**
   * Lists the plans created for a part, newest first. Create them first with **Create a plan** (`POST /parts/{id}/plans`); a part with no plans returns an empty page.  **Early access.** Machining plans and toolpath calculation are available now but still gaining functionality — planned additions include plan constraints and specifying material and stock, among others. Breaking changes still follow the API major version, so you can build against them today; expect new capabilities to arrive as they mature.
   * List plans
   */
  async listPlansRaw(
    requestParameters: ListPlansRequest,
    initOverrides?: RequestInit | runtime.InitOverrideFunction,
  ): Promise<runtime.ApiResponse<PlanListResponse>> {
    const requestOptions = await this.listPlansRequestOpts(requestParameters)
    const response = await this.request(requestOptions, initOverrides)

    return new runtime.JSONApiResponse(response, (jsonValue) => PlanListResponseFromJSON(jsonValue))
  }

  /**
   * Lists the plans created for a part, newest first. Create them first with **Create a plan** (`POST /parts/{id}/plans`); a part with no plans returns an empty page.  **Early access.** Machining plans and toolpath calculation are available now but still gaining functionality — planned additions include plan constraints and specifying material and stock, among others. Breaking changes still follow the API major version, so you can build against them today; expect new capabilities to arrive as they mature.
   * List plans
   */
  async listPlans(
    requestParameters: ListPlansRequest,
    initOverrides?: RequestInit | runtime.InitOverrideFunction,
  ): Promise<PlanListResponse> {
    const response = await this.listPlansRaw(requestParameters, initOverrides)
    return await response.value()
  }
}
