/* tslint:disable */
/* eslint-disable */
/**
 * Toolpath Engine API
 * Public REST API for Toolpath engine operations.  **Early access.** Machining plans and toolpath calculation — creating, listing, and reading plans, calculating toolpaths, and reading toolpath results and machining time — are available now but still gaining functionality; planned additions include plan constraints and specifying material and stock, among others. Breaking changes still follow the API major version, so you can build against them today. These operations are grouped under the `Plans` and `Toolpaths` tags and marked `x-toolpath-experimental`.  **Units.** All dimensional values — in parts and in feature details — are in millimetres; all angles are in degrees. Each part response also states this in its `units` field.  **Browser use.** CORS is per API key: a request only receives an `Access-Control-Allow-Origin` header when its `Origin` is in the allowlist configured on the key used (set when creating or editing the key in the portal). A key with no allowed origins is server-to-server only. A Bearer API key placed in client-side JavaScript is readable by anyone — restrict its origins, prefer a key granted read access only (writes made with it are rejected with 403 `read_only_key`), or proxy requests through your own server.  **Access and billing.** An API key is granted read and write access separately in each area of the API — the core API (uploads, part display, holders, and jobs), DFM (part analysis, feature datasheets, and plans), and Quoting (toolpaths and machining time) — and that access can be edited on an existing key in the portal. Every operation states its area in its `x-toolpath-product` field (`core`, `dfm`, or `quoting`) and whether the call is billed in `x-toolpath-metered`. Reads are free but still belong to their product: reading a plan needs DFM access, and reading toolpaths needs Quoting. `GET` reads; every other method writes. A key with no access to the operation’s area is refused with 403 `product_mismatch`; one with read access used for a write is refused with 403 `read_only_key`.
 *
 * The version of the OpenAPI document: 1.12.1
 *
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */

import * as runtime from '../runtime.js'
import { type JobDetail, JobDetailFromJSON, JobDetailToJSON } from '../models/JobDetail.js'
import {
  type ListJobsResponse,
  ListJobsResponseFromJSON,
  ListJobsResponseToJSON,
} from '../models/ListJobsResponse.js'
import {
  type ProblemDetails,
  ProblemDetailsFromJSON,
  ProblemDetailsToJSON,
} from '../models/ProblemDetails.js'

export interface GetJobRequest {
  id: string
}

export interface ListJobsRequest {
  page?: number
  pageSize?: number
  partId?: string
  holderId?: string
  status?: ListJobsStatusEnum
}

export interface StreamJobEventsRequest {
  id: string
}

/**
 *
 */
export class JobsApi extends runtime.BaseAPI {
  /**
   * Creates request options for getJob without sending the request
   */
  async getJobRequestOpts(requestParameters: GetJobRequest): Promise<runtime.RequestOpts> {
    if (requestParameters['id'] == null) {
      throw new runtime.RequiredError(
        'id',
        'Required parameter "id" was null or undefined when calling getJob().',
      )
    }

    const queryParameters: any = {}

    const headerParameters: runtime.HTTPHeaders = {}

    if (this.configuration && this.configuration.accessToken) {
      const token = this.configuration.accessToken
      const tokenString = await token('ApiKeyAuth', [])

      if (tokenString) {
        headerParameters['Authorization'] = `Bearer ${tokenString}`
      }
    }

    let urlPath = `/v1/jobs/{id}`
    urlPath = urlPath.replace('{id}', encodeURIComponent(String(requestParameters['id'])))

    return {
      path: urlPath,
      method: 'GET',
      headers: headerParameters,
      query: queryParameters,
    }
  }

  /**
   * Returns one processing job. After any pipeline step that queues work, poll this (or **Stream job updates**, `GET /jobs/{id}/events`) until the status is `succeeded`, then read the result from the matching Get endpoint.
   * Get a job
   */
  async getJobRaw(
    requestParameters: GetJobRequest,
    initOverrides?: RequestInit | runtime.InitOverrideFunction,
  ): Promise<runtime.ApiResponse<JobDetail>> {
    const requestOptions = await this.getJobRequestOpts(requestParameters)
    const response = await this.request(requestOptions, initOverrides)

    return new runtime.JSONApiResponse(response, (jsonValue) => JobDetailFromJSON(jsonValue))
  }

  /**
   * Returns one processing job. After any pipeline step that queues work, poll this (or **Stream job updates**, `GET /jobs/{id}/events`) until the status is `succeeded`, then read the result from the matching Get endpoint.
   * Get a job
   */
  async getJob(
    requestParameters: GetJobRequest,
    initOverrides?: RequestInit | runtime.InitOverrideFunction,
  ): Promise<JobDetail> {
    const response = await this.getJobRaw(requestParameters, initOverrides)
    return await response.value()
  }

  /**
   * Creates request options for listJobs without sending the request
   */
  async listJobsRequestOpts(requestParameters: ListJobsRequest): Promise<runtime.RequestOpts> {
    const queryParameters: any = {}

    if (requestParameters['page'] != null) {
      queryParameters['page'] = requestParameters['page']
    }

    if (requestParameters['pageSize'] != null) {
      queryParameters['pageSize'] = requestParameters['pageSize']
    }

    if (requestParameters['partId'] != null) {
      queryParameters['partId'] = requestParameters['partId']
    }

    if (requestParameters['holderId'] != null) {
      queryParameters['holderId'] = requestParameters['holderId']
    }

    if (requestParameters['status'] != null) {
      queryParameters['status'] = requestParameters['status']
    }

    const headerParameters: runtime.HTTPHeaders = {}

    if (this.configuration && this.configuration.accessToken) {
      const token = this.configuration.accessToken
      const tokenString = await token('ApiKeyAuth', [])

      if (tokenString) {
        headerParameters['Authorization'] = `Bearer ${tokenString}`
      }
    }

    let urlPath = `/v1/jobs`

    return {
      path: urlPath,
      method: 'GET',
      headers: headerParameters,
      query: queryParameters,
    }
  }

  /**
   * Lists processing jobs, newest first. Every pipeline step that queues work — **Analyze a part**, **Analyze part features**, **Create a plan**, **Calculate toolpaths**, and **Import a tool holder** — returns a job you track here.
   * List jobs
   */
  async listJobsRaw(
    requestParameters: ListJobsRequest,
    initOverrides?: RequestInit | runtime.InitOverrideFunction,
  ): Promise<runtime.ApiResponse<ListJobsResponse>> {
    const requestOptions = await this.listJobsRequestOpts(requestParameters)
    const response = await this.request(requestOptions, initOverrides)

    return new runtime.JSONApiResponse(response, (jsonValue) => ListJobsResponseFromJSON(jsonValue))
  }

  /**
   * Lists processing jobs, newest first. Every pipeline step that queues work — **Analyze a part**, **Analyze part features**, **Create a plan**, **Calculate toolpaths**, and **Import a tool holder** — returns a job you track here.
   * List jobs
   */
  async listJobs(
    requestParameters: ListJobsRequest = {},
    initOverrides?: RequestInit | runtime.InitOverrideFunction,
  ): Promise<ListJobsResponse> {
    const response = await this.listJobsRaw(requestParameters, initOverrides)
    return await response.value()
  }

  /**
   * Creates request options for streamJobEvents without sending the request
   */
  async streamJobEventsRequestOpts(
    requestParameters: StreamJobEventsRequest,
  ): Promise<runtime.RequestOpts> {
    if (requestParameters['id'] == null) {
      throw new runtime.RequiredError(
        'id',
        'Required parameter "id" was null or undefined when calling streamJobEvents().',
      )
    }

    const queryParameters: any = {}

    const headerParameters: runtime.HTTPHeaders = {}

    if (this.configuration && this.configuration.accessToken) {
      const token = this.configuration.accessToken
      const tokenString = await token('ApiKeyAuth', [])

      if (tokenString) {
        headerParameters['Authorization'] = `Bearer ${tokenString}`
      }
    }

    let urlPath = `/v1/jobs/{id}/events`
    urlPath = urlPath.replace('{id}', encodeURIComponent(String(requestParameters['id'])))

    return {
      path: urlPath,
      method: 'GET',
      headers: headerParameters,
      query: queryParameters,
    }
  }

  /**
   * Sends the current job immediately, then sends every subsequent persisted status, progress, or error change as a `job` server-sent event while the connection remains open. If the connection closes, reconnect to receive the latest job snapshot before continuing with future updates.
   * Stream job updates
   */
  async streamJobEventsRaw(
    requestParameters: StreamJobEventsRequest,
    initOverrides?: RequestInit | runtime.InitOverrideFunction,
  ): Promise<runtime.ApiResponse<string>> {
    const requestOptions = await this.streamJobEventsRequestOpts(requestParameters)
    const response = await this.request(requestOptions, initOverrides)

    if (this.isJsonMime(response.headers.get('content-type'))) {
      return new runtime.JSONApiResponse<string>(response)
    } else {
      return new runtime.TextApiResponse(response) as any
    }
  }

  /**
   * Sends the current job immediately, then sends every subsequent persisted status, progress, or error change as a `job` server-sent event while the connection remains open. If the connection closes, reconnect to receive the latest job snapshot before continuing with future updates.
   * Stream job updates
   */
  async streamJobEvents(
    requestParameters: StreamJobEventsRequest,
    initOverrides?: RequestInit | runtime.InitOverrideFunction,
  ): Promise<string> {
    const response = await this.streamJobEventsRaw(requestParameters, initOverrides)
    return await response.value()
  }
}

/**
 * @export
 */
export const ListJobsStatusEnum = {
  Queued: 'queued',
  Running: 'running',
  Succeeded: 'succeeded',
  Failed: 'failed',
} as const
export type ListJobsStatusEnum = (typeof ListJobsStatusEnum)[keyof typeof ListJobsStatusEnum]
