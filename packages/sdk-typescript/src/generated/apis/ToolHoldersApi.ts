/* tslint:disable */
/* eslint-disable */
/**
 * Toolpath Engine API
 * Public REST API for Toolpath engine operations.  **Early access.** Machining plans and toolpath calculation — creating, listing, and reading plans, calculating toolpaths, and reading toolpath results and machining time — are available now but still gaining functionality; planned additions include plan constraints and specifying material and stock, among others. Breaking changes still follow the API major version, so you can build against them today. These operations are grouped under the `Plans` and `Toolpaths` tags and marked `x-toolpath-experimental`.  **Units.** All dimensional values — in parts and in feature details — are in millimetres; all angles are in degrees. Each part response also states this in its `units` field.  **Browser use.** CORS is per API key: a request only receives an `Access-Control-Allow-Origin` header when its `Origin` is in the allowlist configured on the key used (set when creating or editing the key in the portal). A key with no allowed origins is server-to-server only. A Bearer API key placed in client-side JavaScript is readable by anyone — restrict its origins, prefer a key granted read access only (writes made with it are rejected with 403 `read_only_key`), or proxy requests through your own server.  **Access and billing.** An API key is granted read and write access separately in each area of the API — the core API (uploads, part display, holders, and jobs), DFM (part analysis, feature datasheets, and plans), and Quoting (toolpaths and machining time) — and that access can be edited on an existing key in the portal. Every operation states its area in its `x-toolpath-product` field (`core`, `dfm`, or `quoting`) and whether the call is billed in `x-toolpath-metered`. Reads are free but still belong to their product: reading a plan needs DFM access, and reading toolpaths needs Quoting. `GET` reads; every other method writes. A key with no access to the operation’s area is refused with 403 `product_mismatch`; one with read access used for a write is refused with 403 `read_only_key`.
 *
 * The version of the OpenAPI document: 1.10.0
 *
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */

import * as runtime from '../runtime.js'
import {
  type CreateHolderResponse,
  CreateHolderResponseFromJSON,
  CreateHolderResponseToJSON,
} from '../models/CreateHolderResponse.js'
import {
  type HolderResponse,
  HolderResponseFromJSON,
  HolderResponseToJSON,
} from '../models/HolderResponse.js'
import {
  type ProblemDetails,
  ProblemDetailsFromJSON,
  ProblemDetailsToJSON,
} from '../models/ProblemDetails.js'
import {
  type UpdateHolderResponse,
  UpdateHolderResponseFromJSON,
  UpdateHolderResponseToJSON,
} from '../models/UpdateHolderResponse.js'

export interface CreateHolderRequest {
  filename?: string
}

export interface DownloadHolderFusionRequest {
  id: string
  jobId?: string
  format?: DownloadHolderFusionFormatEnum
  trim?: DownloadHolderFusionTrimEnum
  description?: string
  vendor?: string
  productId?: string
  productLink?: string
}

export interface ExportFusionHolderLibraryRequest {
  ids: string
  trim?: ExportFusionHolderLibraryTrimEnum
}

export interface GetHolderRequest {
  id: string
  jobId?: string
}

export interface UpdateHolderRequest {
  id: string
  tolerance?: number
  fillBays?: UpdateHolderFillBaysEnum
  flipped?: UpdateHolderFlippedEnum
  idempotencyKey?: string
}

/**
 *
 */
export class ToolHoldersApi extends runtime.BaseAPI {
  /**
   * Creates request options for createHolder without sending the request
   */
  async createHolderRequestOpts(
    requestParameters: CreateHolderRequest,
  ): Promise<runtime.RequestOpts> {
    const queryParameters: any = {}

    if (requestParameters['filename'] != null) {
      queryParameters['filename'] = requestParameters['filename']
    }

    const headerParameters: runtime.HTTPHeaders = {}

    if (this.configuration && this.configuration.accessToken) {
      const token = this.configuration.accessToken
      const tokenString = await token('ApiKeyAuth', [])

      if (tokenString) {
        headerParameters['Authorization'] = `Bearer ${tokenString}`
      }
    }

    let urlPath = `/v1/holders`

    return {
      path: urlPath,
      method: 'POST',
      headers: headerParameters,
      query: queryParameters,
    }
  }

  /**
   * Creates a tool holder and returns a short-lived URL for uploading its CAD source. Upload the file to the URL, then run **Import a tool holder** (`PATCH /holders/{id}`).
   * Upload a tool holder
   */
  async createHolderRaw(
    requestParameters: CreateHolderRequest,
    initOverrides?: RequestInit | runtime.InitOverrideFunction,
  ): Promise<runtime.ApiResponse<CreateHolderResponse>> {
    const requestOptions = await this.createHolderRequestOpts(requestParameters)
    const response = await this.request(requestOptions, initOverrides)

    return new runtime.JSONApiResponse(response, (jsonValue) =>
      CreateHolderResponseFromJSON(jsonValue),
    )
  }

  /**
   * Creates a tool holder and returns a short-lived URL for uploading its CAD source. Upload the file to the URL, then run **Import a tool holder** (`PATCH /holders/{id}`).
   * Upload a tool holder
   */
  async createHolder(
    requestParameters: CreateHolderRequest = {},
    initOverrides?: RequestInit | runtime.InitOverrideFunction,
  ): Promise<CreateHolderResponse> {
    const response = await this.createHolderRaw(requestParameters, initOverrides)
    return await response.value()
  }

  /**
   * Creates request options for downloadHolderFusion without sending the request
   */
  async downloadHolderFusionRequestOpts(
    requestParameters: DownloadHolderFusionRequest,
  ): Promise<runtime.RequestOpts> {
    if (requestParameters['id'] == null) {
      throw new runtime.RequiredError(
        'id',
        'Required parameter "id" was null or undefined when calling downloadHolderFusion().',
      )
    }

    const queryParameters: any = {}

    if (requestParameters['jobId'] != null) {
      queryParameters['jobId'] = requestParameters['jobId']
    }

    if (requestParameters['format'] != null) {
      queryParameters['format'] = requestParameters['format']
    }

    if (requestParameters['trim'] != null) {
      queryParameters['trim'] = requestParameters['trim']
    }

    if (requestParameters['description'] != null) {
      queryParameters['description'] = requestParameters['description']
    }

    if (requestParameters['vendor'] != null) {
      queryParameters['vendor'] = requestParameters['vendor']
    }

    if (requestParameters['productId'] != null) {
      queryParameters['productId'] = requestParameters['productId']
    }

    if (requestParameters['productLink'] != null) {
      queryParameters['productLink'] = requestParameters['productLink']
    }

    const headerParameters: runtime.HTTPHeaders = {}

    if (this.configuration && this.configuration.accessToken) {
      const token = this.configuration.accessToken
      const tokenString = await token('ApiKeyAuth', [])

      if (tokenString) {
        headerParameters['Authorization'] = `Bearer ${tokenString}`
      }
    }

    let urlPath = `/v1/holders/{id}/fusion`
    urlPath = urlPath.replace('{id}', encodeURIComponent(String(requestParameters['id'])))

    return {
      path: urlPath,
      method: 'GET',
      headers: headerParameters,
      query: queryParameters,
    }
  }

  /**
   * Requires **Import a tool holder** (`PATCH /holders/{id}`) to have run and its job to have succeeded.  Returns the derived holder as Autodesk Fusion writes one: millimetres, segments nose-first, hyphenated keys.  By default this is a **tool library** (`format=library`) that Fusion can import directly. `format=document` returns the bare holder document the kernel wrote, which carries the same geometry but cannot be imported on its own.  The exported envelope stops at the gauge plane by default: material above it is inside the spindle and cannot collide with anything. Pass `trim=false` for the complete envelope, including the taper. Both are written by the kernel during the import, so neither is a reshaping of the other. `trim=true` falls back to the complete envelope for a holder that has no gauge plane to cut at, or whose taper a flip put at the nose.  An unmeasured gauge length is not written as zero. In a library it becomes the sum of every segment height — Fusion\'s own idiom for a holder whose geometry is its gauge length; in a document the field is omitted, which puts Fusion\'s own field into manual mode.
   * Download a holder (Fusion)
   */
  async downloadHolderFusionRaw(
    requestParameters: DownloadHolderFusionRequest,
    initOverrides?: RequestInit | runtime.InitOverrideFunction,
  ): Promise<runtime.ApiResponse<string>> {
    const requestOptions = await this.downloadHolderFusionRequestOpts(requestParameters)
    const response = await this.request(requestOptions, initOverrides)

    if (this.isJsonMime(response.headers.get('content-type'))) {
      return new runtime.JSONApiResponse<string>(response)
    } else {
      return new runtime.TextApiResponse(response) as any
    }
  }

  /**
   * Requires **Import a tool holder** (`PATCH /holders/{id}`) to have run and its job to have succeeded.  Returns the derived holder as Autodesk Fusion writes one: millimetres, segments nose-first, hyphenated keys.  By default this is a **tool library** (`format=library`) that Fusion can import directly. `format=document` returns the bare holder document the kernel wrote, which carries the same geometry but cannot be imported on its own.  The exported envelope stops at the gauge plane by default: material above it is inside the spindle and cannot collide with anything. Pass `trim=false` for the complete envelope, including the taper. Both are written by the kernel during the import, so neither is a reshaping of the other. `trim=true` falls back to the complete envelope for a holder that has no gauge plane to cut at, or whose taper a flip put at the nose.  An unmeasured gauge length is not written as zero. In a library it becomes the sum of every segment height — Fusion\'s own idiom for a holder whose geometry is its gauge length; in a document the field is omitted, which puts Fusion\'s own field into manual mode.
   * Download a holder (Fusion)
   */
  async downloadHolderFusion(
    requestParameters: DownloadHolderFusionRequest,
    initOverrides?: RequestInit | runtime.InitOverrideFunction,
  ): Promise<string> {
    const response = await this.downloadHolderFusionRaw(requestParameters, initOverrides)
    return await response.value()
  }

  /**
   * Creates request options for exportFusionHolderLibrary without sending the request
   */
  async exportFusionHolderLibraryRequestOpts(
    requestParameters: ExportFusionHolderLibraryRequest,
  ): Promise<runtime.RequestOpts> {
    if (requestParameters['ids'] == null) {
      throw new runtime.RequiredError(
        'ids',
        'Required parameter "ids" was null or undefined when calling exportFusionHolderLibrary().',
      )
    }

    const queryParameters: any = {}

    if (requestParameters['ids'] != null) {
      queryParameters['ids'] = requestParameters['ids']
    }

    if (requestParameters['trim'] != null) {
      queryParameters['trim'] = requestParameters['trim']
    }

    const headerParameters: runtime.HTTPHeaders = {}

    if (this.configuration && this.configuration.accessToken) {
      const token = this.configuration.accessToken
      const tokenString = await token('ApiKeyAuth', [])

      if (tokenString) {
        headerParameters['Authorization'] = `Bearer ${tokenString}`
      }
    }

    let urlPath = `/v1/holder-libraries/fusion`

    return {
      path: urlPath,
      method: 'GET',
      headers: headerParameters,
      query: queryParameters,
    }
  }

  /**
   * Composes the named holders into a single Autodesk Fusion tool library — how a shop keeps its crib in one file. Each holder must already have been imported; this reads their stored results and creates nothing.  Holders appear in the order they were named, and a repeated id is exported once. Each entry takes its name from the holder’s uploaded filename.  Every named holder has to be exportable. A holder that does not exist, or that has no import result yet, fails the whole request and is named in the response — a library that silently contained fewer holders than were asked for would be worse than an error.
   * Export a holder library (Fusion)
   */
  async exportFusionHolderLibraryRaw(
    requestParameters: ExportFusionHolderLibraryRequest,
    initOverrides?: RequestInit | runtime.InitOverrideFunction,
  ): Promise<runtime.ApiResponse<string>> {
    const requestOptions = await this.exportFusionHolderLibraryRequestOpts(requestParameters)
    const response = await this.request(requestOptions, initOverrides)

    if (this.isJsonMime(response.headers.get('content-type'))) {
      return new runtime.JSONApiResponse<string>(response)
    } else {
      return new runtime.TextApiResponse(response) as any
    }
  }

  /**
   * Composes the named holders into a single Autodesk Fusion tool library — how a shop keeps its crib in one file. Each holder must already have been imported; this reads their stored results and creates nothing.  Holders appear in the order they were named, and a repeated id is exported once. Each entry takes its name from the holder’s uploaded filename.  Every named holder has to be exportable. A holder that does not exist, or that has no import result yet, fails the whole request and is named in the response — a library that silently contained fewer holders than were asked for would be worse than an error.
   * Export a holder library (Fusion)
   */
  async exportFusionHolderLibrary(
    requestParameters: ExportFusionHolderLibraryRequest,
    initOverrides?: RequestInit | runtime.InitOverrideFunction,
  ): Promise<string> {
    const response = await this.exportFusionHolderLibraryRaw(requestParameters, initOverrides)
    return await response.value()
  }

  /**
   * Creates request options for getHolder without sending the request
   */
  async getHolderRequestOpts(requestParameters: GetHolderRequest): Promise<runtime.RequestOpts> {
    if (requestParameters['id'] == null) {
      throw new runtime.RequiredError(
        'id',
        'Required parameter "id" was null or undefined when calling getHolder().',
      )
    }

    const queryParameters: any = {}

    if (requestParameters['jobId'] != null) {
      queryParameters['jobId'] = requestParameters['jobId']
    }

    const headerParameters: runtime.HTTPHeaders = {}

    if (this.configuration && this.configuration.accessToken) {
      const token = this.configuration.accessToken
      const tokenString = await token('ApiKeyAuth', [])

      if (tokenString) {
        headerParameters['Authorization'] = `Bearer ${tokenString}`
      }
    }

    let urlPath = `/v1/holders/{id}`
    urlPath = urlPath.replace('{id}', encodeURIComponent(String(requestParameters['id'])))

    return {
      path: urlPath,
      method: 'GET',
      headers: headerParameters,
      query: queryParameters,
    }
  }

  /**
   * Returns a tool holder’s imported geometry and collision envelope. Requires **Import a tool holder** (`PATCH /holders/{id}`) to have run and its job to have succeeded.
   * Get a tool holder
   */
  async getHolderRaw(
    requestParameters: GetHolderRequest,
    initOverrides?: RequestInit | runtime.InitOverrideFunction,
  ): Promise<runtime.ApiResponse<HolderResponse>> {
    const requestOptions = await this.getHolderRequestOpts(requestParameters)
    const response = await this.request(requestOptions, initOverrides)

    return new runtime.JSONApiResponse(response, (jsonValue) => HolderResponseFromJSON(jsonValue))
  }

  /**
   * Returns a tool holder’s imported geometry and collision envelope. Requires **Import a tool holder** (`PATCH /holders/{id}`) to have run and its job to have succeeded.
   * Get a tool holder
   */
  async getHolder(
    requestParameters: GetHolderRequest,
    initOverrides?: RequestInit | runtime.InitOverrideFunction,
  ): Promise<HolderResponse> {
    const response = await this.getHolderRaw(requestParameters, initOverrides)
    return await response.value()
  }

  /**
   * Creates request options for updateHolder without sending the request
   */
  async updateHolderRequestOpts(
    requestParameters: UpdateHolderRequest,
  ): Promise<runtime.RequestOpts> {
    if (requestParameters['id'] == null) {
      throw new runtime.RequiredError(
        'id',
        'Required parameter "id" was null or undefined when calling updateHolder().',
      )
    }

    const queryParameters: any = {}

    if (requestParameters['tolerance'] != null) {
      queryParameters['tolerance'] = requestParameters['tolerance']
    }

    if (requestParameters['fillBays'] != null) {
      queryParameters['fillBays'] = requestParameters['fillBays']
    }

    if (requestParameters['flipped'] != null) {
      queryParameters['flipped'] = requestParameters['flipped']
    }

    const headerParameters: runtime.HTTPHeaders = {}

    if (requestParameters['idempotencyKey'] != null) {
      headerParameters['Idempotency-Key'] = String(requestParameters['idempotencyKey'])
    }

    if (this.configuration && this.configuration.accessToken) {
      const token = this.configuration.accessToken
      const tokenString = await token('ApiKeyAuth', [])

      if (tokenString) {
        headerParameters['Authorization'] = `Bearer ${tokenString}`
      }
    }

    let urlPath = `/v1/holders/{id}`
    urlPath = urlPath.replace('{id}', encodeURIComponent(String(requestParameters['id'])))

    return {
      path: urlPath,
      method: 'PATCH',
      headers: headerParameters,
      query: queryParameters,
    }
  }

  /**
   * Queues import of an uploaded tool holder — deriving its collision envelope — and returns a job. Track the job, then read the result with **Get a tool holder** (`GET /holders/{id}`).
   * Import a tool holder
   */
  async updateHolderRaw(
    requestParameters: UpdateHolderRequest,
    initOverrides?: RequestInit | runtime.InitOverrideFunction,
  ): Promise<runtime.ApiResponse<UpdateHolderResponse>> {
    const requestOptions = await this.updateHolderRequestOpts(requestParameters)
    const response = await this.request(requestOptions, initOverrides)

    return new runtime.JSONApiResponse(response, (jsonValue) =>
      UpdateHolderResponseFromJSON(jsonValue),
    )
  }

  /**
   * Queues import of an uploaded tool holder — deriving its collision envelope — and returns a job. Track the job, then read the result with **Get a tool holder** (`GET /holders/{id}`).
   * Import a tool holder
   */
  async updateHolder(
    requestParameters: UpdateHolderRequest,
    initOverrides?: RequestInit | runtime.InitOverrideFunction,
  ): Promise<UpdateHolderResponse> {
    const response = await this.updateHolderRaw(requestParameters, initOverrides)
    return await response.value()
  }
}

/**
 * @export
 */
export const DownloadHolderFusionFormatEnum = {
  Library: 'library',
  Document: 'document',
} as const
export type DownloadHolderFusionFormatEnum =
  (typeof DownloadHolderFusionFormatEnum)[keyof typeof DownloadHolderFusionFormatEnum]
/**
 * @export
 */
export const DownloadHolderFusionTrimEnum = {
  True: 'true',
  False: 'false',
} as const
export type DownloadHolderFusionTrimEnum =
  (typeof DownloadHolderFusionTrimEnum)[keyof typeof DownloadHolderFusionTrimEnum]
/**
 * @export
 */
export const ExportFusionHolderLibraryTrimEnum = {
  True: 'true',
  False: 'false',
} as const
export type ExportFusionHolderLibraryTrimEnum =
  (typeof ExportFusionHolderLibraryTrimEnum)[keyof typeof ExportFusionHolderLibraryTrimEnum]
/**
 * @export
 */
export const UpdateHolderFillBaysEnum = {
  True: 'true',
  False: 'false',
} as const
export type UpdateHolderFillBaysEnum =
  (typeof UpdateHolderFillBaysEnum)[keyof typeof UpdateHolderFillBaysEnum]
/**
 * @export
 */
export const UpdateHolderFlippedEnum = {
  True: 'true',
  False: 'false',
} as const
export type UpdateHolderFlippedEnum =
  (typeof UpdateHolderFlippedEnum)[keyof typeof UpdateHolderFlippedEnum]
