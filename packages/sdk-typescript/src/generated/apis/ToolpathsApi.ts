/* tslint:disable */
/* eslint-disable */
/**
 * Toolpath Engine API
 * Public REST API for Toolpath engine operations.  **Early access.** Machining plans and toolpath calculation — creating, listing, and reading plans, calculating toolpaths, and reading toolpath results and machining time — are available now but still gaining functionality; planned additions include plan constraints and specifying material and stock, among others. Breaking changes still follow the API major version, so you can build against them today. These operations are grouped under the `Plans` and `Toolpaths` tags and marked `x-toolpath-experimental`.  **Units.** All dimensional values — in parts and in feature details — are in millimetres; all angles are in degrees. Each part response also states this in its `units` field.  **Browser use.** CORS is per API key: a request only receives an `Access-Control-Allow-Origin` header when its `Origin` is in the allowlist configured on the key used (set when creating or editing the key in the portal). A key with no allowed origins is server-to-server only. A Bearer API key placed in client-side JavaScript is readable by anyone — restrict its origins, prefer a key granted read access only (writes made with it are rejected with 403 `read_only_key`), or proxy requests through your own server.  **Access and billing.** An API key is granted read and write access separately in each area of the API — the core API (uploads, part display, holders, and jobs), DFM (part analysis, feature datasheets, and plans), and Quoting (toolpaths and machining time) — and that access can be edited on an existing key in the portal. Every operation states its area in its `x-toolpath-product` field (`core`, `dfm`, or `quoting`) and whether the call is billed in `x-toolpath-metered`. Reads are free but still belong to their product: reading a plan needs DFM access, and reading toolpaths needs Quoting. `GET` reads; every other method writes. A key with no access to the operation’s area is refused with 403 `product_mismatch`; one with read access used for a write is refused with 403 `read_only_key`.
 *
 * The version of the OpenAPI document: 1.10.0
 *
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */

import * as runtime from '../runtime.js'
import {
  type MachiningTimeResponse,
  MachiningTimeResponseFromJSON,
  MachiningTimeResponseToJSON,
} from '../models/MachiningTimeResponse.js'
import {
  type PipelineReadinessProblem,
  PipelineReadinessProblemFromJSON,
  PipelineReadinessProblemToJSON,
} from '../models/PipelineReadinessProblem.js'
import {
  type ProblemDetails,
  ProblemDetailsFromJSON,
  ProblemDetailsToJSON,
} from '../models/ProblemDetails.js'
import {
  type QueuePartJobResponse,
  QueuePartJobResponseFromJSON,
  QueuePartJobResponseToJSON,
} from '../models/QueuePartJobResponse.js'
import {
  type ToolpathsResponse,
  ToolpathsResponseFromJSON,
  ToolpathsResponseToJSON,
} from '../models/ToolpathsResponse.js'

export interface CreateToolpathsRequest {
  id: string
  idempotencyKey?: string
}

export interface GetMachiningTimeRequest {
  planId: string
}

export interface GetToolpathsRequest {
  planId: string
}

export interface RecalculateToolpathsRequest {
  planId: string
  idempotencyKey?: string
}

/**
 *
 */
export class ToolpathsApi extends runtime.BaseAPI {
  /**
   * Creates request options for createToolpaths without sending the request
   */
  async createToolpathsRequestOpts(
    requestParameters: CreateToolpathsRequest,
  ): Promise<runtime.RequestOpts> {
    if (requestParameters['id'] == null) {
      throw new runtime.RequiredError(
        'id',
        'Required parameter "id" was null or undefined when calling createToolpaths().',
      )
    }

    const queryParameters: any = {}

    const headerParameters: runtime.HTTPHeaders = {}

    if (requestParameters['idempotencyKey'] != null) {
      headerParameters['Idempotency-Key'] = String(requestParameters['idempotencyKey'])
    }

    if (this.configuration && this.configuration.accessToken) {
      const token = this.configuration.accessToken
      const tokenString = await token('ApiKeyAuth', [])

      if (tokenString) {
        headerParameters['Authorization'] = `Bearer ${tokenString}`
      }
    }

    let urlPath = `/v1/parts/{id}/toolpaths`
    urlPath = urlPath.replace('{id}', encodeURIComponent(String(requestParameters['id'])))

    return {
      path: urlPath,
      method: 'POST',
      headers: headerParameters,
      query: queryParameters,
    }
  }

  /**
   * Creates a plan and calculates its toolpaths in one step, returning a job. Track the job, then read the result with **Get toolpaths** (`GET /plans/{planId}/toolpaths`) and **Get machining time** (`GET /plans/{planId}/machining-time`).  **Early access.** Machining plans and toolpath calculation are available now but still gaining functionality — planned additions include plan constraints and specifying material and stock, among others. Breaking changes still follow the API major version, so you can build against them today; expect new capabilities to arrive as they mature.
   * Calculate toolpaths
   */
  async createToolpathsRaw(
    requestParameters: CreateToolpathsRequest,
    initOverrides?: RequestInit | runtime.InitOverrideFunction,
  ): Promise<runtime.ApiResponse<QueuePartJobResponse>> {
    const requestOptions = await this.createToolpathsRequestOpts(requestParameters)
    const response = await this.request(requestOptions, initOverrides)

    return new runtime.JSONApiResponse(response, (jsonValue) =>
      QueuePartJobResponseFromJSON(jsonValue),
    )
  }

  /**
   * Creates a plan and calculates its toolpaths in one step, returning a job. Track the job, then read the result with **Get toolpaths** (`GET /plans/{planId}/toolpaths`) and **Get machining time** (`GET /plans/{planId}/machining-time`).  **Early access.** Machining plans and toolpath calculation are available now but still gaining functionality — planned additions include plan constraints and specifying material and stock, among others. Breaking changes still follow the API major version, so you can build against them today; expect new capabilities to arrive as they mature.
   * Calculate toolpaths
   */
  async createToolpaths(
    requestParameters: CreateToolpathsRequest,
    initOverrides?: RequestInit | runtime.InitOverrideFunction,
  ): Promise<QueuePartJobResponse> {
    const response = await this.createToolpathsRaw(requestParameters, initOverrides)
    return await response.value()
  }

  /**
   * Creates request options for getMachiningTime without sending the request
   */
  async getMachiningTimeRequestOpts(
    requestParameters: GetMachiningTimeRequest,
  ): Promise<runtime.RequestOpts> {
    if (requestParameters['planId'] == null) {
      throw new runtime.RequiredError(
        'planId',
        'Required parameter "planId" was null or undefined when calling getMachiningTime().',
      )
    }

    const queryParameters: any = {}

    const headerParameters: runtime.HTTPHeaders = {}

    if (this.configuration && this.configuration.accessToken) {
      const token = this.configuration.accessToken
      const tokenString = await token('ApiKeyAuth', [])

      if (tokenString) {
        headerParameters['Authorization'] = `Bearer ${tokenString}`
      }
    }

    let urlPath = `/v1/plans/{planId}/machining-time`
    urlPath = urlPath.replace('{planId}', encodeURIComponent(String(requestParameters['planId'])))

    return {
      path: urlPath,
      method: 'GET',
      headers: headerParameters,
      query: queryParameters,
    }
  }

  /**
   * Returns the machining time for a plan’s toolpaths, per action and totalled. Requires **Calculate toolpaths** (`POST /parts/{id}/toolpaths`) or **Recalculate toolpaths** (`POST /plans/{planId}/toolpaths`) to have run and its job to have succeeded.  **Early access.** Machining plans and toolpath calculation are available now but still gaining functionality — planned additions include plan constraints and specifying material and stock, among others. Breaking changes still follow the API major version, so you can build against them today; expect new capabilities to arrive as they mature.
   * Get machining time
   */
  async getMachiningTimeRaw(
    requestParameters: GetMachiningTimeRequest,
    initOverrides?: RequestInit | runtime.InitOverrideFunction,
  ): Promise<runtime.ApiResponse<MachiningTimeResponse>> {
    const requestOptions = await this.getMachiningTimeRequestOpts(requestParameters)
    const response = await this.request(requestOptions, initOverrides)

    return new runtime.JSONApiResponse(response, (jsonValue) =>
      MachiningTimeResponseFromJSON(jsonValue),
    )
  }

  /**
   * Returns the machining time for a plan’s toolpaths, per action and totalled. Requires **Calculate toolpaths** (`POST /parts/{id}/toolpaths`) or **Recalculate toolpaths** (`POST /plans/{planId}/toolpaths`) to have run and its job to have succeeded.  **Early access.** Machining plans and toolpath calculation are available now but still gaining functionality — planned additions include plan constraints and specifying material and stock, among others. Breaking changes still follow the API major version, so you can build against them today; expect new capabilities to arrive as they mature.
   * Get machining time
   */
  async getMachiningTime(
    requestParameters: GetMachiningTimeRequest,
    initOverrides?: RequestInit | runtime.InitOverrideFunction,
  ): Promise<MachiningTimeResponse> {
    const response = await this.getMachiningTimeRaw(requestParameters, initOverrides)
    return await response.value()
  }

  /**
   * Creates request options for getToolpaths without sending the request
   */
  async getToolpathsRequestOpts(
    requestParameters: GetToolpathsRequest,
  ): Promise<runtime.RequestOpts> {
    if (requestParameters['planId'] == null) {
      throw new runtime.RequiredError(
        'planId',
        'Required parameter "planId" was null or undefined when calling getToolpaths().',
      )
    }

    const queryParameters: any = {}

    const headerParameters: runtime.HTTPHeaders = {}

    if (this.configuration && this.configuration.accessToken) {
      const token = this.configuration.accessToken
      const tokenString = await token('ApiKeyAuth', [])

      if (tokenString) {
        headerParameters['Authorization'] = `Bearer ${tokenString}`
      }
    }

    let urlPath = `/v1/plans/{planId}/toolpaths`
    urlPath = urlPath.replace('{planId}', encodeURIComponent(String(requestParameters['planId'])))

    return {
      path: urlPath,
      method: 'GET',
      headers: headerParameters,
      query: queryParameters,
    }
  }

  /**
   * Returns the calculated toolpaths for a plan, with geometry download URLs. Requires **Calculate toolpaths** (`POST /parts/{id}/toolpaths`) or **Recalculate toolpaths** (`POST /plans/{planId}/toolpaths`) to have run and its job to have succeeded.  **Early access.** Machining plans and toolpath calculation are available now but still gaining functionality — planned additions include plan constraints and specifying material and stock, among others. Breaking changes still follow the API major version, so you can build against them today; expect new capabilities to arrive as they mature.
   * Get toolpaths
   */
  async getToolpathsRaw(
    requestParameters: GetToolpathsRequest,
    initOverrides?: RequestInit | runtime.InitOverrideFunction,
  ): Promise<runtime.ApiResponse<ToolpathsResponse>> {
    const requestOptions = await this.getToolpathsRequestOpts(requestParameters)
    const response = await this.request(requestOptions, initOverrides)

    return new runtime.JSONApiResponse(response, (jsonValue) =>
      ToolpathsResponseFromJSON(jsonValue),
    )
  }

  /**
   * Returns the calculated toolpaths for a plan, with geometry download URLs. Requires **Calculate toolpaths** (`POST /parts/{id}/toolpaths`) or **Recalculate toolpaths** (`POST /plans/{planId}/toolpaths`) to have run and its job to have succeeded.  **Early access.** Machining plans and toolpath calculation are available now but still gaining functionality — planned additions include plan constraints and specifying material and stock, among others. Breaking changes still follow the API major version, so you can build against them today; expect new capabilities to arrive as they mature.
   * Get toolpaths
   */
  async getToolpaths(
    requestParameters: GetToolpathsRequest,
    initOverrides?: RequestInit | runtime.InitOverrideFunction,
  ): Promise<ToolpathsResponse> {
    const response = await this.getToolpathsRaw(requestParameters, initOverrides)
    return await response.value()
  }

  /**
   * Creates request options for recalculateToolpaths without sending the request
   */
  async recalculateToolpathsRequestOpts(
    requestParameters: RecalculateToolpathsRequest,
  ): Promise<runtime.RequestOpts> {
    if (requestParameters['planId'] == null) {
      throw new runtime.RequiredError(
        'planId',
        'Required parameter "planId" was null or undefined when calling recalculateToolpaths().',
      )
    }

    const queryParameters: any = {}

    const headerParameters: runtime.HTTPHeaders = {}

    if (requestParameters['idempotencyKey'] != null) {
      headerParameters['Idempotency-Key'] = String(requestParameters['idempotencyKey'])
    }

    if (this.configuration && this.configuration.accessToken) {
      const token = this.configuration.accessToken
      const tokenString = await token('ApiKeyAuth', [])

      if (tokenString) {
        headerParameters['Authorization'] = `Bearer ${tokenString}`
      }
    }

    let urlPath = `/v1/plans/{planId}/toolpaths`
    urlPath = urlPath.replace('{planId}', encodeURIComponent(String(requestParameters['planId'])))

    return {
      path: urlPath,
      method: 'POST',
      headers: headerParameters,
      query: queryParameters,
    }
  }

  /**
   * Calculates toolpaths for an existing plan and returns a job — for a workflow that creates a plan first with **Create a plan** (`POST /parts/{id}/plans`) and calculates its toolpaths afterward, and for recomputing a plan’s toolpaths. Track the job, then read the result with **Get toolpaths** (`GET /plans/{planId}/toolpaths`) and **Get machining time** (`GET /plans/{planId}/machining-time`).  **Early access.** Machining plans and toolpath calculation are available now but still gaining functionality — planned additions include plan constraints and specifying material and stock, among others. Breaking changes still follow the API major version, so you can build against them today; expect new capabilities to arrive as they mature.
   * Calculate a plan’s toolpaths
   */
  async recalculateToolpathsRaw(
    requestParameters: RecalculateToolpathsRequest,
    initOverrides?: RequestInit | runtime.InitOverrideFunction,
  ): Promise<runtime.ApiResponse<QueuePartJobResponse>> {
    const requestOptions = await this.recalculateToolpathsRequestOpts(requestParameters)
    const response = await this.request(requestOptions, initOverrides)

    return new runtime.JSONApiResponse(response, (jsonValue) =>
      QueuePartJobResponseFromJSON(jsonValue),
    )
  }

  /**
   * Calculates toolpaths for an existing plan and returns a job — for a workflow that creates a plan first with **Create a plan** (`POST /parts/{id}/plans`) and calculates its toolpaths afterward, and for recomputing a plan’s toolpaths. Track the job, then read the result with **Get toolpaths** (`GET /plans/{planId}/toolpaths`) and **Get machining time** (`GET /plans/{planId}/machining-time`).  **Early access.** Machining plans and toolpath calculation are available now but still gaining functionality — planned additions include plan constraints and specifying material and stock, among others. Breaking changes still follow the API major version, so you can build against them today; expect new capabilities to arrive as they mature.
   * Calculate a plan’s toolpaths
   */
  async recalculateToolpaths(
    requestParameters: RecalculateToolpathsRequest,
    initOverrides?: RequestInit | runtime.InitOverrideFunction,
  ): Promise<QueuePartJobResponse> {
    const response = await this.recalculateToolpathsRaw(requestParameters, initOverrides)
    return await response.value()
  }
}
