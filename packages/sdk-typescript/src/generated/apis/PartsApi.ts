/* tslint:disable */
/* eslint-disable */
/**
 * Toolpath Engine API
 * Public REST API for Toolpath engine operations.  **Early access.** Machining plans and toolpath calculation — creating, listing, and reading plans, calculating toolpaths, and reading toolpath results and machining time — are available now but still gaining functionality; planned additions include plan constraints and specifying material and stock, among others. Breaking changes still follow the API major version, so you can build against them today. These operations are grouped under the `Plans` and `Toolpaths` tags and marked `x-toolpath-experimental`.  **Units.** All dimensional values — in parts and in feature details — are in millimetres; all angles are in degrees. Each part response also states this in its `units` field.  **Browser use.** CORS is per API key: a request only receives an `Access-Control-Allow-Origin` header when its `Origin` is in the allowlist configured on the key used (set when creating or editing the key in the portal). A key with no allowed origins is server-to-server only. A Bearer API key placed in client-side JavaScript is readable by anyone — restrict its origins, prefer a key granted read access only (writes made with it are rejected with 403 `read_only_key`), or proxy requests through your own server.  **Access and billing.** An API key is granted read and write access separately in each area of the API — the core API (uploads, part display, holders, and jobs), DFM (part analysis, feature datasheets, and plans), and Quoting (toolpaths and machining time) — and that access can be edited on an existing key in the portal. Every operation states its area in its `x-toolpath-product` field (`core`, `dfm`, or `quoting`) and whether the call is billed in `x-toolpath-metered`. Reads are free but still belong to their product: reading a plan needs DFM access, and reading toolpaths needs Quoting. `GET` reads; every other method writes. A key with no access to the operation’s area is refused with 403 `product_mismatch`; one with read access used for a write is refused with 403 `read_only_key`.
 *
 * The version of the OpenAPI document: 1.10.0
 *
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */

import * as runtime from '../runtime.js'
import {
  type CreatePartResponse,
  CreatePartResponseFromJSON,
  CreatePartResponseToJSON,
} from '../models/CreatePartResponse.js'
import {
  type PartMeshJobResponse,
  PartMeshJobResponseFromJSON,
  PartMeshJobResponseToJSON,
} from '../models/PartMeshJobResponse.js'
import {
  type PartMeshResponse,
  PartMeshResponseFromJSON,
  PartMeshResponseToJSON,
} from '../models/PartMeshResponse.js'
import {
  type PartResponse,
  PartResponseFromJSON,
  PartResponseToJSON,
} from '../models/PartResponse.js'
import {
  type ProblemDetails,
  ProblemDetailsFromJSON,
  ProblemDetailsToJSON,
} from '../models/ProblemDetails.js'
import {
  type UpdatePartResponse,
  UpdatePartResponseFromJSON,
  UpdatePartResponseToJSON,
} from '../models/UpdatePartResponse.js'

export interface CreatePartRequest {
  filename?: string
}

export interface CreatePartMeshRequest {
  id: string
  idempotencyKey?: string
}

export interface GetPartRequest {
  id: string
  jobId?: string
}

export interface GetPartMeshRequest {
  id: string
  jobId?: string
}

export interface GetPartThumbnailRequest {
  id: string
  jobId?: string
}

export interface UpdatePartRequest {
  id: string
  featureDetails?: UpdatePartFeatureDetailsEnum
  idempotencyKey?: string
}

/**
 *
 */
export class PartsApi extends runtime.BaseAPI {
  /**
   * Creates request options for createPart without sending the request
   */
  async createPartRequestOpts(requestParameters: CreatePartRequest): Promise<runtime.RequestOpts> {
    const queryParameters: any = {}

    if (requestParameters['filename'] != null) {
      queryParameters['filename'] = requestParameters['filename']
    }

    const headerParameters: runtime.HTTPHeaders = {}

    if (this.configuration && this.configuration.accessToken) {
      const token = this.configuration.accessToken
      const tokenString = await token('ApiKeyAuth', [])

      if (tokenString) {
        headerParameters['Authorization'] = `Bearer ${tokenString}`
      }
    }

    let urlPath = `/v1/parts`

    return {
      path: urlPath,
      method: 'POST',
      headers: headerParameters,
      query: queryParameters,
    }
  }

  /**
   * Creates a part and returns a short-lived URL for uploading its CAD source. This is the first step of the pipeline — upload the file to the URL, then run **Analyze a part** (`PATCH /parts/{id}`).
   * Upload a part
   */
  async createPartRaw(
    requestParameters: CreatePartRequest,
    initOverrides?: RequestInit | runtime.InitOverrideFunction,
  ): Promise<runtime.ApiResponse<CreatePartResponse>> {
    const requestOptions = await this.createPartRequestOpts(requestParameters)
    const response = await this.request(requestOptions, initOverrides)

    return new runtime.JSONApiResponse(response, (jsonValue) =>
      CreatePartResponseFromJSON(jsonValue),
    )
  }

  /**
   * Creates a part and returns a short-lived URL for uploading its CAD source. This is the first step of the pipeline — upload the file to the URL, then run **Analyze a part** (`PATCH /parts/{id}`).
   * Upload a part
   */
  async createPart(
    requestParameters: CreatePartRequest = {},
    initOverrides?: RequestInit | runtime.InitOverrideFunction,
  ): Promise<CreatePartResponse> {
    const response = await this.createPartRaw(requestParameters, initOverrides)
    return await response.value()
  }

  /**
   * Creates request options for createPartMesh without sending the request
   */
  async createPartMeshRequestOpts(
    requestParameters: CreatePartMeshRequest,
  ): Promise<runtime.RequestOpts> {
    if (requestParameters['id'] == null) {
      throw new runtime.RequiredError(
        'id',
        'Required parameter "id" was null or undefined when calling createPartMesh().',
      )
    }

    const queryParameters: any = {}

    const headerParameters: runtime.HTTPHeaders = {}

    if (requestParameters['idempotencyKey'] != null) {
      headerParameters['Idempotency-Key'] = String(requestParameters['idempotencyKey'])
    }

    if (this.configuration && this.configuration.accessToken) {
      const token = this.configuration.accessToken
      const tokenString = await token('ApiKeyAuth', [])

      if (tokenString) {
        headerParameters['Authorization'] = `Bearer ${tokenString}`
      }
    }

    let urlPath = `/v1/parts/{id}/mesh`
    urlPath = urlPath.replace('{id}', encodeURIComponent(String(requestParameters['id'])))

    return {
      path: urlPath,
      method: 'POST',
      headers: headerParameters,
      query: queryParameters,
    }
  }

  /**
   * Queues a job that facets the uploaded CAD file into a display mesh without analyzing it — the fast path for a viewer that only needs to draw the part. Poll the job, then read the mesh at GET /parts/{id}/mesh. Each run replaces the part’s previous display mesh.
   * Tessellate a part for display
   */
  async createPartMeshRaw(
    requestParameters: CreatePartMeshRequest,
    initOverrides?: RequestInit | runtime.InitOverrideFunction,
  ): Promise<runtime.ApiResponse<PartMeshJobResponse>> {
    const requestOptions = await this.createPartMeshRequestOpts(requestParameters)
    const response = await this.request(requestOptions, initOverrides)

    return new runtime.JSONApiResponse(response, (jsonValue) =>
      PartMeshJobResponseFromJSON(jsonValue),
    )
  }

  /**
   * Queues a job that facets the uploaded CAD file into a display mesh without analyzing it — the fast path for a viewer that only needs to draw the part. Poll the job, then read the mesh at GET /parts/{id}/mesh. Each run replaces the part’s previous display mesh.
   * Tessellate a part for display
   */
  async createPartMesh(
    requestParameters: CreatePartMeshRequest,
    initOverrides?: RequestInit | runtime.InitOverrideFunction,
  ): Promise<PartMeshJobResponse> {
    const response = await this.createPartMeshRaw(requestParameters, initOverrides)
    return await response.value()
  }

  /**
   * Creates request options for getPart without sending the request
   */
  async getPartRequestOpts(requestParameters: GetPartRequest): Promise<runtime.RequestOpts> {
    if (requestParameters['id'] == null) {
      throw new runtime.RequiredError(
        'id',
        'Required parameter "id" was null or undefined when calling getPart().',
      )
    }

    const queryParameters: any = {}

    if (requestParameters['jobId'] != null) {
      queryParameters['jobId'] = requestParameters['jobId']
    }

    const headerParameters: runtime.HTTPHeaders = {}

    if (this.configuration && this.configuration.accessToken) {
      const token = this.configuration.accessToken
      const tokenString = await token('ApiKeyAuth', [])

      if (tokenString) {
        headerParameters['Authorization'] = `Bearer ${tokenString}`
      }
    }

    let urlPath = `/v1/parts/{id}`
    urlPath = urlPath.replace('{id}', encodeURIComponent(String(requestParameters['id'])))

    return {
      path: urlPath,
      method: 'GET',
      headers: headerParameters,
      query: queryParameters,
    }
  }

  /**
   * Returns a part and the results of its machining analysis. Requires **Analyze a part** (`PATCH /parts/{id}`) to have run and its job to have succeeded; until then there is no result and this returns 404.
   * Get a part
   */
  async getPartRaw(
    requestParameters: GetPartRequest,
    initOverrides?: RequestInit | runtime.InitOverrideFunction,
  ): Promise<runtime.ApiResponse<PartResponse>> {
    const requestOptions = await this.getPartRequestOpts(requestParameters)
    const response = await this.request(requestOptions, initOverrides)

    return new runtime.JSONApiResponse(response, (jsonValue) => PartResponseFromJSON(jsonValue))
  }

  /**
   * Returns a part and the results of its machining analysis. Requires **Analyze a part** (`PATCH /parts/{id}`) to have run and its job to have succeeded; until then there is no result and this returns 404.
   * Get a part
   */
  async getPart(
    requestParameters: GetPartRequest,
    initOverrides?: RequestInit | runtime.InitOverrideFunction,
  ): Promise<PartResponse> {
    const response = await this.getPartRaw(requestParameters, initOverrides)
    return await response.value()
  }

  /**
   * Creates request options for getPartMesh without sending the request
   */
  async getPartMeshRequestOpts(
    requestParameters: GetPartMeshRequest,
  ): Promise<runtime.RequestOpts> {
    if (requestParameters['id'] == null) {
      throw new runtime.RequiredError(
        'id',
        'Required parameter "id" was null or undefined when calling getPartMesh().',
      )
    }

    const queryParameters: any = {}

    if (requestParameters['jobId'] != null) {
      queryParameters['jobId'] = requestParameters['jobId']
    }

    const headerParameters: runtime.HTTPHeaders = {}

    if (this.configuration && this.configuration.accessToken) {
      const token = this.configuration.accessToken
      const tokenString = await token('ApiKeyAuth', [])

      if (tokenString) {
        headerParameters['Authorization'] = `Bearer ${tokenString}`
      }
    }

    let urlPath = `/v1/parts/{id}/mesh`
    urlPath = urlPath.replace('{id}', encodeURIComponent(String(requestParameters['id'])))

    return {
      path: urlPath,
      method: 'GET',
      headers: headerParameters,
      query: queryParameters,
    }
  }

  /**
   * Returns the display mesh the latest tessellation job wrote for this part. Requires **Tessellate a part for display** (`POST /parts/{id}/mesh`) to have run and its job to have succeeded. It is faceted from the uploaded file as-is, so it is not the analysis mesh: its counts differ from the part result’s, and region triangle ranges do not apply to it. 410 once the part is past the retention window.
   * Get part mesh
   */
  async getPartMeshRaw(
    requestParameters: GetPartMeshRequest,
    initOverrides?: RequestInit | runtime.InitOverrideFunction,
  ): Promise<runtime.ApiResponse<PartMeshResponse>> {
    const requestOptions = await this.getPartMeshRequestOpts(requestParameters)
    const response = await this.request(requestOptions, initOverrides)

    return new runtime.JSONApiResponse(response, (jsonValue) => PartMeshResponseFromJSON(jsonValue))
  }

  /**
   * Returns the display mesh the latest tessellation job wrote for this part. Requires **Tessellate a part for display** (`POST /parts/{id}/mesh`) to have run and its job to have succeeded. It is faceted from the uploaded file as-is, so it is not the analysis mesh: its counts differ from the part result’s, and region triangle ranges do not apply to it. 410 once the part is past the retention window.
   * Get part mesh
   */
  async getPartMesh(
    requestParameters: GetPartMeshRequest,
    initOverrides?: RequestInit | runtime.InitOverrideFunction,
  ): Promise<PartMeshResponse> {
    const response = await this.getPartMeshRaw(requestParameters, initOverrides)
    return await response.value()
  }

  /**
   * Creates request options for getPartThumbnail without sending the request
   */
  async getPartThumbnailRequestOpts(
    requestParameters: GetPartThumbnailRequest,
  ): Promise<runtime.RequestOpts> {
    if (requestParameters['id'] == null) {
      throw new runtime.RequiredError(
        'id',
        'Required parameter "id" was null or undefined when calling getPartThumbnail().',
      )
    }

    const queryParameters: any = {}

    if (requestParameters['jobId'] != null) {
      queryParameters['jobId'] = requestParameters['jobId']
    }

    const headerParameters: runtime.HTTPHeaders = {}

    if (this.configuration && this.configuration.accessToken) {
      const token = this.configuration.accessToken
      const tokenString = await token('ApiKeyAuth', [])

      if (tokenString) {
        headerParameters['Authorization'] = `Bearer ${tokenString}`
      }
    }

    let urlPath = `/v1/parts/{id}/thumbnail`
    urlPath = urlPath.replace('{id}', encodeURIComponent(String(requestParameters['id'])))

    return {
      path: urlPath,
      method: 'GET',
      headers: headerParameters,
      query: queryParameters,
    }
  }

  /**
   * Requires **Analyze a part** (`PATCH /parts/{id}`) to have run and its job to have succeeded.  Serves the rendered PNG thumbnail for a part as image bytes, so a client can fetch it with the API key it already holds rather than reading `thumbnailUrl` off `GET /v1/parts/{id}` first. The thumbnail is the kernel’s three-quarter view of the analyzed part.  The bytes are served through this request rather than as a redirect to a presigned URL: a thumbnail is a few kB, and streaming it keeps the disclosure on this request’s access audit instead of handing out a bearer grant that outlives it. `thumbnailUrl` on the part response is still the right choice for an `<img src>`, which cannot carry an Authorization header.  Resolves the same artifact `GET /v1/parts/{id}` reports — the latest analysis run by default, or the run named by `?jobId=`. A part whose analysis produced no thumbnail answers 404.
   * Get part thumbnail
   */
  async getPartThumbnailRaw(
    requestParameters: GetPartThumbnailRequest,
    initOverrides?: RequestInit | runtime.InitOverrideFunction,
  ): Promise<runtime.ApiResponse<Blob>> {
    const requestOptions = await this.getPartThumbnailRequestOpts(requestParameters)
    const response = await this.request(requestOptions, initOverrides)

    return new runtime.BlobApiResponse(response)
  }

  /**
   * Requires **Analyze a part** (`PATCH /parts/{id}`) to have run and its job to have succeeded.  Serves the rendered PNG thumbnail for a part as image bytes, so a client can fetch it with the API key it already holds rather than reading `thumbnailUrl` off `GET /v1/parts/{id}` first. The thumbnail is the kernel’s three-quarter view of the analyzed part.  The bytes are served through this request rather than as a redirect to a presigned URL: a thumbnail is a few kB, and streaming it keeps the disclosure on this request’s access audit instead of handing out a bearer grant that outlives it. `thumbnailUrl` on the part response is still the right choice for an `<img src>`, which cannot carry an Authorization header.  Resolves the same artifact `GET /v1/parts/{id}` reports — the latest analysis run by default, or the run named by `?jobId=`. A part whose analysis produced no thumbnail answers 404.
   * Get part thumbnail
   */
  async getPartThumbnail(
    requestParameters: GetPartThumbnailRequest,
    initOverrides?: RequestInit | runtime.InitOverrideFunction,
  ): Promise<Blob> {
    const response = await this.getPartThumbnailRaw(requestParameters, initOverrides)
    return await response.value()
  }

  /**
   * Creates request options for updatePart without sending the request
   */
  async updatePartRequestOpts(requestParameters: UpdatePartRequest): Promise<runtime.RequestOpts> {
    if (requestParameters['id'] == null) {
      throw new runtime.RequiredError(
        'id',
        'Required parameter "id" was null or undefined when calling updatePart().',
      )
    }

    const queryParameters: any = {}

    if (requestParameters['featureDetails'] != null) {
      queryParameters['featureDetails'] = requestParameters['featureDetails']
    }

    const headerParameters: runtime.HTTPHeaders = {}

    if (requestParameters['idempotencyKey'] != null) {
      headerParameters['Idempotency-Key'] = String(requestParameters['idempotencyKey'])
    }

    if (this.configuration && this.configuration.accessToken) {
      const token = this.configuration.accessToken
      const tokenString = await token('ApiKeyAuth', [])

      if (tokenString) {
        headerParameters['Authorization'] = `Bearer ${tokenString}`
      }
    }

    let urlPath = `/v1/parts/{id}`
    urlPath = urlPath.replace('{id}', encodeURIComponent(String(requestParameters['id'])))

    return {
      path: urlPath,
      method: 'PATCH',
      headers: headerParameters,
      query: queryParameters,
    }
  }

  /**
   * Queues machining analysis for an uploaded part and returns a job. Track the job, then read the result with **Get a part** (`GET /parts/{id}`).
   * Analyze a part
   */
  async updatePartRaw(
    requestParameters: UpdatePartRequest,
    initOverrides?: RequestInit | runtime.InitOverrideFunction,
  ): Promise<runtime.ApiResponse<UpdatePartResponse>> {
    const requestOptions = await this.updatePartRequestOpts(requestParameters)
    const response = await this.request(requestOptions, initOverrides)

    return new runtime.JSONApiResponse(response, (jsonValue) =>
      UpdatePartResponseFromJSON(jsonValue),
    )
  }

  /**
   * Queues machining analysis for an uploaded part and returns a job. Track the job, then read the result with **Get a part** (`GET /parts/{id}`).
   * Analyze a part
   */
  async updatePart(
    requestParameters: UpdatePartRequest,
    initOverrides?: RequestInit | runtime.InitOverrideFunction,
  ): Promise<UpdatePartResponse> {
    const response = await this.updatePartRaw(requestParameters, initOverrides)
    return await response.value()
  }
}

/**
 * @export
 */
export const UpdatePartFeatureDetailsEnum = {
  True: 'true',
  False: 'false',
} as const
export type UpdatePartFeatureDetailsEnum =
  (typeof UpdatePartFeatureDetailsEnum)[keyof typeof UpdatePartFeatureDetailsEnum]
