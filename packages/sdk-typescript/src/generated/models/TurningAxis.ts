/* tslint:disable */
/* eslint-disable */
/**
 * Toolpath Engine API
 * Public REST API for Toolpath engine operations.  **Units.** All dimensional values — in parts and in feature details — are in millimetres; all angles are in degrees. Each part response also states this in its `units` field.  **Browser use.** CORS is per API key: a request only receives an `Access-Control-Allow-Origin` header when its `Origin` is in the allowlist configured on the key used (set when creating or editing the key in the portal). A key with no allowed origins is server-to-server only. A Bearer API key placed in client-side JavaScript is readable by anyone — restrict its origins, prefer a key granted read access only (writes made with it are rejected with 403 `read_only_key`), or proxy requests through your own server.  **Access and billing.** An API key is granted read and write access separately in each area of the API — the core API (uploads, reads, holders, and jobs), DFM, and Quoting — and that access can be edited on an existing key in the portal. Every operation states what it is billed as in its `x-toolpath-billing` field: `dfm` or `quoting` for a metered operation, which records usage, or `unmetered` for a core operation. `GET` reads; every other method writes. A key with no access to the operation’s area is refused with 403 `product_mismatch`; one with read access used for a write is refused with 403 `read_only_key`.
 *
 * The version of the OpenAPI document: 1.9.0
 *
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */

import { mapValues } from '../runtime.js'
import type { TurningAxisLocation } from './TurningAxisLocation.js'
import {
  TurningAxisLocationFromJSON,
  TurningAxisLocationFromJSONTyped,
  TurningAxisLocationToJSON,
  TurningAxisLocationToJSONTyped,
} from './TurningAxisLocation.js'
import type { TurningAxisDirection } from './TurningAxisDirection.js'
import {
  TurningAxisDirectionFromJSON,
  TurningAxisDirectionFromJSONTyped,
  TurningAxisDirectionToJSON,
  TurningAxisDirectionToJSONTyped,
} from './TurningAxisDirection.js'

/**
 *
 * @export
 * @interface TurningAxis
 */
export interface TurningAxis {
  /**
   * Discriminator identifying a part with a turning axis.
   * @type {TurningAxisKindEnum}
   * @memberof TurningAxis
   */
  kind: TurningAxisKindEnum
  /**
   *
   * @type {TurningAxisDirection}
   * @memberof TurningAxis
   */
  direction: TurningAxisDirection
  /**
   *
   * @type {TurningAxisLocation}
   * @memberof TurningAxis
   */
  location: TurningAxisLocation
  /**
   * Fraction of the part’s surface area a turning setup about this axis could finish, from 0 to 1 — the area lying on the part’s envelope of revolution within tolerance.
   * @type {number}
   * @memberof TurningAxis
   */
  areaFraction: number
  /**
   * Fraction of the part’s envelope of revolution — what comes off the lathe — that the part keeps, from 0 to 1. The rest is what a turning setup leaves to be cut away some other way, so a radially unbalanced part scores low here however much of its surface lies on the envelope.
   * @type {number}
   * @memberof TurningAxis
   */
  volumeFraction: number
}

/**
 * @export
 */
export const TurningAxisKindEnum = {
  TurningAxis: 'TurningAxis',
} as const
export type TurningAxisKindEnum = (typeof TurningAxisKindEnum)[keyof typeof TurningAxisKindEnum]

/**
 * Check if a given object implements the TurningAxis interface.
 */
export function instanceOfTurningAxis(value: object): value is TurningAxis {
  if (!('kind' in value) || value['kind'] === undefined) return false
  if (value['kind'] !== 'TurningAxis') return false

  if (!('direction' in value) || value['direction'] === undefined) return false
  if (!('location' in value) || value['location'] === undefined) return false
  if (!('areaFraction' in value) || value['areaFraction'] === undefined) return false
  if (!('volumeFraction' in value) || value['volumeFraction'] === undefined) return false
  return true
}

export function TurningAxisFromJSON(json: any): TurningAxis {
  return TurningAxisFromJSONTyped(json, false)
}

export function TurningAxisFromJSONTyped(json: any, ignoreDiscriminator: boolean): TurningAxis {
  if (json == null) {
    return json
  }
  return {
    kind: json['kind'],
    direction: TurningAxisDirectionFromJSON(json['direction']),
    location: TurningAxisLocationFromJSON(json['location']),
    areaFraction: json['areaFraction'],
    volumeFraction: json['volumeFraction'],
  }
}

export function TurningAxisToJSON(json: any): TurningAxis {
  return TurningAxisToJSONTyped(json, false)
}

export function TurningAxisToJSONTyped(
  value?: TurningAxis | null,
  ignoreDiscriminator: boolean = false,
): any {
  if (value == null) {
    return value
  }

  return {
    kind: value['kind'],
    direction: TurningAxisDirectionToJSON(value['direction']),
    location: TurningAxisLocationToJSON(value['location']),
    areaFraction: value['areaFraction'],
    volumeFraction: value['volumeFraction'],
  }
}
