/* tslint:disable */
/* eslint-disable */
/**
 * Toolpath Engine API
 * Public REST API for Toolpath engine operations.  **Early access.** Machining plans and toolpath calculation — creating, listing, and reading plans, calculating toolpaths, and reading toolpath results and machining time — are available now but still gaining functionality; planned additions include plan constraints and specifying material and stock, among others. Breaking changes still follow the API major version, so you can build against them today. These operations are grouped under the `Plans` and `Toolpaths` tags and marked `x-toolpath-experimental`.  **Units.** All dimensional values — in parts and in feature details — are in millimetres; all angles are in degrees. Each part response also states this in its `units` field.  **Browser use.** CORS is per API key: a request only receives an `Access-Control-Allow-Origin` header when its `Origin` is in the allowlist configured on the key used (set when creating or editing the key in the portal). A key with no allowed origins is server-to-server only. A Bearer API key placed in client-side JavaScript is readable by anyone — restrict its origins, prefer a key granted read access only (writes made with it are rejected with 403 `read_only_key`), or proxy requests through your own server.  **Access and billing.** An API key is granted read and write access separately in each area of the API — the core API (uploads, part display, holders, and jobs), DFM (part analysis, feature datasheets, and plans), and Quoting (toolpaths and machining time) — and that access can be edited on an existing key in the portal. Every operation states its area in its `x-toolpath-product` field (`core`, `dfm`, or `quoting`) and whether the call is billed in `x-toolpath-metered`. Reads are free but still belong to their product: reading a plan needs DFM access, and reading toolpaths needs Quoting. `GET` reads; every other method writes. A key with no access to the operation’s area is refused with 403 `product_mismatch`; one with read access used for a write is refused with 403 `read_only_key`.
 *
 * The version of the OpenAPI document: 1.12.1
 *
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */

import { mapValues } from '../runtime.js'
/**
 * An outline a pass follows, measured so that any tool's path length can be read off it.
 *
 * A pass does not walk the outline: it walks the tool *center*, offset by the tool radius to
 * the free side, which is shorter than the outline inside a pocket and longer around a boss.
 * So the length a tool of `radius` travels is `Math.max(0, length + radius * dlDr)`, and zero
 * means the tool does not fit — the outline is shorter than the tool's own orbit, which a
 * caller pricing a pass must refuse rather than bill as free.
 *
 * Exact for every tool the outline has room for, except at a corner sharper than the tool,
 * where it reads long by that corner's miter — zero for the filleted corners a pocket that
 * admits the tool is made of.
 * @export
 * @interface OffsetLength
 */
export interface OffsetLength {
  /**
   * The outline's own length, in mm — what a pass would travel with a tool of no width.
   * @type {number}
   * @memberof OffsetLength
   */
  length: number
  /**
   * How the travelled length changes per mm of tool radius: the outline's total signed
   * turning, in radians and so dimensionless. Negative around an outline a tool runs
   * inside of, positive around one it runs outside of, and `±2π` where the outline closes.
   * @type {number}
   * @memberof OffsetLength
   */
  dlDr: number
}

/**
 * Check if a given object implements the OffsetLength interface.
 */
export function instanceOfOffsetLength(value: object): value is OffsetLength {
  if (!('length' in value) || value['length'] === undefined) return false
  if (!('dlDr' in value) || value['dlDr'] === undefined) return false
  return true
}

export function OffsetLengthFromJSON(json: any): OffsetLength {
  return OffsetLengthFromJSONTyped(json, false)
}

export function OffsetLengthFromJSONTyped(json: any, ignoreDiscriminator: boolean): OffsetLength {
  if (json == null) {
    return json
  }
  return {
    length: json['length'],
    dlDr: json['dlDr'],
  }
}

export function OffsetLengthToJSON(json: any): OffsetLength {
  return OffsetLengthToJSONTyped(json, false)
}

export function OffsetLengthToJSONTyped(
  value?: OffsetLength | null,
  ignoreDiscriminator: boolean = false,
): any {
  if (value == null) {
    return value
  }

  return {
    length: value['length'],
    dlDr: value['dlDr'],
  }
}
