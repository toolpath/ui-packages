/* tslint:disable */
/* eslint-disable */
/**
 * Toolpath Engine API
 * Public REST API for Toolpath engine operations.  **Units.** All dimensional values — in parts and in feature details — are in millimetres; all angles are in degrees. Each part response also states this in its `units` field.  **Browser use.** CORS is per API key: a request only receives an `Access-Control-Allow-Origin` header when its `Origin` is in the allowlist configured on the key used (set when creating or editing the key in the portal). A key with no allowed origins is server-to-server only. A Bearer API key placed in client-side JavaScript is readable by anyone — restrict its origins, prefer a key granted read access only (writes made with it are rejected with 403 `read_only_key`), or proxy requests through your own server.  **Access and billing.** An API key is granted read and write access separately in each area of the API — the core API (uploads, reads, holders, and jobs), DFM, and Quoting — and that access can be edited on an existing key in the portal. Every operation states what it is billed as in its `x-toolpath-billing` field: `dfm` or `quoting` for a metered operation, which records usage, or `unmetered` for a core operation. `GET` reads; every other method writes. A key with no access to the operation’s area is refused with 403 `product_mismatch`; one with read access used for a write is refused with 403 `read_only_key`.
 *
 * The version of the OpenAPI document: 1.9.0
 *
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */

import { mapValues } from '../runtime.js'
/**
 *
 * @export
 * @interface KeyValidationResponse
 */
export interface KeyValidationResponse {
  /**
   * Whether the key is usable right now.
   * @type {boolean}
   * @memberof KeyValidationResponse
   */
  valid: boolean
  /**
   * The derived key status: active (usable), revoked (disabled), expired (past expiry), or invalid (unknown key).
   * @type {KeyValidationResponseStatusEnum}
   * @memberof KeyValidationResponse
   */
  status: KeyValidationResponseStatusEnum
}

/**
 * @export
 */
export const KeyValidationResponseStatusEnum = {
  Active: 'active',
  Revoked: 'revoked',
  Expired: 'expired',
  Invalid: 'invalid',
} as const
export type KeyValidationResponseStatusEnum =
  (typeof KeyValidationResponseStatusEnum)[keyof typeof KeyValidationResponseStatusEnum]

/**
 * Check if a given object implements the KeyValidationResponse interface.
 */
export function instanceOfKeyValidationResponse(value: object): value is KeyValidationResponse {
  if (!('valid' in value) || value['valid'] === undefined) return false
  if (!('status' in value) || value['status'] === undefined) return false
  return true
}

export function KeyValidationResponseFromJSON(json: any): KeyValidationResponse {
  return KeyValidationResponseFromJSONTyped(json, false)
}

export function KeyValidationResponseFromJSONTyped(
  json: any,
  ignoreDiscriminator: boolean,
): KeyValidationResponse {
  if (json == null) {
    return json
  }
  return {
    valid: json['valid'],
    status: json['status'],
  }
}

export function KeyValidationResponseToJSON(json: any): KeyValidationResponse {
  return KeyValidationResponseToJSONTyped(json, false)
}

export function KeyValidationResponseToJSONTyped(
  value?: KeyValidationResponse | null,
  ignoreDiscriminator: boolean = false,
): any {
  if (value == null) {
    return value
  }

  return {
    valid: value['valid'],
    status: value['status'],
  }
}
