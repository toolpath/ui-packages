/* tslint:disable */
/* eslint-disable */
/**
 * Toolpath Engine API
 * Public REST API for Toolpath engine operations.  **Early access.** Machining plans and toolpath calculation — creating, listing, and reading plans, calculating toolpaths, and reading toolpath results and machining time — are available now but still gaining functionality; planned additions include plan constraints and specifying material and stock, among others. Breaking changes still follow the API major version, so you can build against them today. These operations are grouped under the `Plans` and `Toolpaths` tags and marked `x-toolpath-experimental`.  **Units.** All dimensional values — in parts and in feature details — are in millimetres; all angles are in degrees. Each part response also states this in its `units` field.  **Browser use.** CORS is per API key: a request only receives an `Access-Control-Allow-Origin` header when its `Origin` is in the allowlist configured on the key used (set when creating or editing the key in the portal). A key with no allowed origins is server-to-server only. A Bearer API key placed in client-side JavaScript is readable by anyone — restrict its origins, prefer a key granted read access only (writes made with it are rejected with 403 `read_only_key`), or proxy requests through your own server.  **Access and billing.** An API key is granted read and write access separately in each area of the API — the core API (uploads, part display, holders, and jobs), DFM (part analysis, feature datasheets, and plans), and Quoting (toolpaths and machining time) — and that access can be edited on an existing key in the portal. Every operation states its area in its `x-toolpath-product` field (`core`, `dfm`, or `quoting`) and whether the call is billed in `x-toolpath-metered`. Reads are free but still belong to their product: reading a plan needs DFM access, and reading toolpaths needs Quoting. `GET` reads; every other method writes. A key with no access to the operation’s area is refused with 403 `product_mismatch`; one with read access used for a write is refused with 403 `read_only_key`.
 *
 * The version of the OpenAPI document: 1.12.1
 *
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */

import { mapValues } from '../runtime.js'
import type { ToolFitResult } from './ToolFitResult.js'
import {
  ToolFitResultFromJSON,
  ToolFitResultFromJSONTyped,
  ToolFitResultToJSON,
  ToolFitResultToJSONTyped,
} from './ToolFitResult.js'
import type { CdData } from './CdData.js'
import { CdDataFromJSON, CdDataFromJSONTyped, CdDataToJSON, CdDataToJSONTyped } from './CdData.js'

/**
 * A three-dimensional surface a tool has to follow rather than sweep; also the surface
 * half of a chamfer.
 * @export
 * @interface SurfaceFacts
 */
export interface SurfaceFacts {
  /**
   * Discriminator for this facts variant.
   * @type {SurfaceFactsKindEnum}
   * @memberof SurfaceFacts
   */
  kind: SurfaceFactsKindEnum
  /**
   * The blend radius, signed by which way the surface turns: positive rolling over
   * an edge, negative running into a corner, zero for no blend at all.
   * @type {number}
   * @memberof SurfaceFacts
   */
  filletRadius: number
  /**
   * The deepest cut taken in one pass down the surface, mm.
   * @type {number}
   * @memberof SurfaceFacts
   */
  maxStepdown: number
  /**
   * How much scallop the finishing pass may leave between neighboring passes, mm.
   * @type {number}
   * @memberof SurfaceFacts
   */
  surfaceFinishCuspHeight: number
  /**
   * Whether the surface includes a sharp corner a tool must respect.
   * @type {boolean}
   * @memberof SurfaceFacts
   */
  hasSharpCorner: boolean
  /**
   * Whether only ball tools are suitable for the finishing pass.
   * @type {boolean}
   * @memberof SurfaceFacts
   */
  useOnlyBallToolsForFinish: boolean
  /**
   * Largest bottom diameter a terminal tool may have, in mm.
   * @type {number}
   * @memberof SurfaceFacts
   */
  maxBottomDiameter: number
  /**
   * Clearance-diameter limits for tools reaching the surface.
   * @type {CdData}
   * @memberof SurfaceFacts
   */
  cd: CdData
  /**
   * Geometry limits imposed by the surface shape itself.
   * @type {ToolFitResult}
   * @memberof SurfaceFacts
   */
  toolFit: ToolFitResult
  /**
   * Deprecated: whether the fillet has a U-shaped cross section. tp-kernel 0.10.0 no longer computes it, so a feature enriched since reads `false`; `useOnlyBallToolsForFinish` is the whole of that verdict. Removed in the next API major.
   * @type {boolean}
   * @memberof SurfaceFacts
   * @deprecated
   */
  isUShapedFillet: boolean
}

/**
 * @export
 */
export const SurfaceFactsKindEnum = {
  Three: 'Three',
} as const
export type SurfaceFactsKindEnum = (typeof SurfaceFactsKindEnum)[keyof typeof SurfaceFactsKindEnum]

/**
 * Check if a given object implements the SurfaceFacts interface.
 */
export function instanceOfSurfaceFacts(value: object): value is SurfaceFacts {
  if (!('kind' in value) || value['kind'] === undefined) return false
  if (value['kind'] !== 'Three') return false

  if (!('filletRadius' in value) || value['filletRadius'] === undefined) return false
  if (!('maxStepdown' in value) || value['maxStepdown'] === undefined) return false
  if (!('surfaceFinishCuspHeight' in value) || value['surfaceFinishCuspHeight'] === undefined)
    return false
  if (!('hasSharpCorner' in value) || value['hasSharpCorner'] === undefined) return false
  if (!('useOnlyBallToolsForFinish' in value) || value['useOnlyBallToolsForFinish'] === undefined)
    return false
  if (!('maxBottomDiameter' in value) || value['maxBottomDiameter'] === undefined) return false
  if (!('cd' in value) || value['cd'] === undefined) return false
  if (!('toolFit' in value) || value['toolFit'] === undefined) return false
  if (!('isUShapedFillet' in value) || value['isUShapedFillet'] === undefined) return false
  return true
}

export function SurfaceFactsFromJSON(json: any): SurfaceFacts {
  return SurfaceFactsFromJSONTyped(json, false)
}

export function SurfaceFactsFromJSONTyped(json: any, ignoreDiscriminator: boolean): SurfaceFacts {
  if (json == null) {
    return json
  }
  return {
    kind: json['kind'],
    filletRadius: json['filletRadius'],
    maxStepdown: json['maxStepdown'],
    surfaceFinishCuspHeight: json['surfaceFinishCuspHeight'],
    hasSharpCorner: json['hasSharpCorner'],
    useOnlyBallToolsForFinish: json['useOnlyBallToolsForFinish'],
    maxBottomDiameter: json['maxBottomDiameter'],
    cd: CdDataFromJSON(json['cd']),
    toolFit: ToolFitResultFromJSON(json['toolFit']),
    isUShapedFillet: json['isUShapedFillet'],
  }
}

export function SurfaceFactsToJSON(json: any): SurfaceFacts {
  return SurfaceFactsToJSONTyped(json, false)
}

export function SurfaceFactsToJSONTyped(
  value?: SurfaceFacts | null,
  ignoreDiscriminator: boolean = false,
): any {
  if (value == null) {
    return value
  }

  return {
    kind: value['kind'],
    filletRadius: value['filletRadius'],
    maxStepdown: value['maxStepdown'],
    surfaceFinishCuspHeight: value['surfaceFinishCuspHeight'],
    hasSharpCorner: value['hasSharpCorner'],
    useOnlyBallToolsForFinish: value['useOnlyBallToolsForFinish'],
    maxBottomDiameter: value['maxBottomDiameter'],
    cd: CdDataToJSON(value['cd']),
    toolFit: ToolFitResultToJSON(value['toolFit']),
    isUShapedFillet: value['isUShapedFillet'],
  }
}
