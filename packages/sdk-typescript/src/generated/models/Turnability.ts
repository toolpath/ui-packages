/* tslint:disable */
/* eslint-disable */
/**
 * Toolpath Engine API
 * Public REST API for Toolpath engine operations.  **Units.** All dimensional values — in parts and in feature details — are in millimetres; all angles are in degrees. Each part response also states this in its `units` field.  **Browser use.** CORS is per API key: a request only receives an `Access-Control-Allow-Origin` header when its `Origin` is in the allowlist configured on the key used (set when creating or editing the key in the portal). A key with no allowed origins is server-to-server only. A Bearer API key placed in client-side JavaScript is readable by anyone — restrict its origins, prefer a key granted read access only (writes made with it are rejected with 403 `read_only_key`), or proxy requests through your own server.  **Access and billing.** An API key is granted read and write access separately in each area of the API — the core API (uploads, reads, holders, and jobs), DFM, and Quoting — and that access can be edited on an existing key in the portal. Every operation states what it is billed as in its `x-toolpath-billing` field: `dfm` or `quoting` for a metered operation, which records usage, or `unmetered` for a core operation. `GET` reads; every other method writes. A key with no access to the operation’s area is refused with 403 `product_mismatch`; one with read access used for a write is refused with 403 `read_only_key`.
 *
 * The version of the OpenAPI document: 1.9.0
 *
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */

import type { NoAxis } from './NoAxis.js'
import { instanceOfNoAxis, NoAxisFromJSON, NoAxisFromJSONTyped, NoAxisToJSON } from './NoAxis.js'
import type { TurningAxis } from './TurningAxis.js'
import {
  instanceOfTurningAxis,
  TurningAxisFromJSON,
  TurningAxisFromJSONTyped,
  TurningAxisToJSON,
} from './TurningAxis.js'

/**
 * @type Turnability
 * Whether the part belongs on a lathe; narrow on `kind`.
 * @export
 */
export type Turnability = ({ kind: 'NoAxis' } & NoAxis) | ({ kind: 'TurningAxis' } & TurningAxis)

export function TurnabilityFromJSON(json: any): Turnability {
  return TurnabilityFromJSONTyped(json, false)
}

export function TurnabilityFromJSONTyped(json: any, ignoreDiscriminator: boolean): Turnability {
  if (json == null) {
    return json
  }
  switch (json['kind']) {
    case 'NoAxis':
      return Object.assign({}, NoAxisFromJSONTyped(json, true), { kind: 'NoAxis' } as const)
    case 'TurningAxis':
      return Object.assign({}, TurningAxisFromJSONTyped(json, true), {
        kind: 'TurningAxis',
      } as const)
    default:
      return json
  }
}

export function TurnabilityToJSON(json: any): any {
  return TurnabilityToJSONTyped(json, false)
}

export function TurnabilityToJSONTyped(
  value?: Turnability | null,
  ignoreDiscriminator: boolean = false,
): any {
  if (value == null) {
    return value
  }
  switch (value['kind']) {
    case 'NoAxis':
      return Object.assign({}, NoAxisToJSON(value), { kind: 'NoAxis' } as const)
    case 'TurningAxis':
      return Object.assign({}, TurningAxisToJSON(value), { kind: 'TurningAxis' } as const)
    default:
      return value
  }
}
