/* tslint:disable */
/* eslint-disable */
/**
 * Toolpath Engine API
 * Public REST API for Toolpath engine operations.  **Units.** All dimensional values — in parts and in feature details — are in millimetres; all angles are in degrees. Each part response also states this in its `units` field.  **Browser use.** CORS is per API key: a request only receives an `Access-Control-Allow-Origin` header when its `Origin` is in the allowlist configured on the key used (set when creating or editing the key in the portal). A key with no allowed origins is server-to-server only. A Bearer API key placed in client-side JavaScript is readable by anyone — restrict its origins, prefer a key granted read access only (writes made with it are rejected with 403 `read_only_key`), or proxy requests through your own server.  **Access and billing.** An API key is granted read and write access separately in each area of the API — the core API (uploads, reads, holders, and jobs), DFM, and Quoting — and that access can be edited on an existing key in the portal. Every operation states what it is billed as in its `x-toolpath-billing` field: `dfm` or `quoting` for a metered operation, which records usage, or `unmetered` for a core operation. `GET` reads; every other method writes. A key with no access to the operation’s area is refused with 403 `product_mismatch`; one with read access used for a write is refused with 403 `read_only_key`.
 *
 * The version of the OpenAPI document: 1.11.0
 *
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */

import { mapValues } from '../runtime.js'
import type { Vec3 } from './Vec3.js'
import { Vec3FromJSON, Vec3FromJSONTyped, Vec3ToJSON, Vec3ToJSONTyped } from './Vec3.js'
import type { PlanAction } from './PlanAction.js'
import {
  PlanActionFromJSON,
  PlanActionFromJSONTyped,
  PlanActionToJSON,
  PlanActionToJSONTyped,
} from './PlanAction.js'

/**
 *
 * @export
 * @interface PlanSetup
 */
export interface PlanSetup {
  /**
   * Position of this item in machining order.
   * @type {number}
   * @memberof PlanSetup
   */
  idx: number
  /**
   * The setup’s machining direction (tool axis), or null when none is exposed.
   * @type {Vec3}
   * @memberof PlanSetup
   */
  direction: Vec3 | null
  /**
   * Action-specs, in machining order.
   * @type {Array<PlanAction>}
   * @memberof PlanSetup
   */
  actions: Array<PlanAction>
  /**
   * Feature tags (hex) this setup was meant to machine that no pass in it does — a coverage loss the tool pool could not finish. Empty when the setup machines everything assigned.
   * @type {Array<string>}
   * @memberof PlanSetup
   */
  unmachined: Array<string>
}

/**
 * Check if a given object implements the PlanSetup interface.
 */
export function instanceOfPlanSetup(value: object): value is PlanSetup {
  if (!('idx' in value) || value['idx'] === undefined) return false
  if (!('direction' in value) || value['direction'] === undefined) return false
  if (!('actions' in value) || value['actions'] === undefined) return false
  if (!('unmachined' in value) || value['unmachined'] === undefined) return false
  return true
}

export function PlanSetupFromJSON(json: any): PlanSetup {
  return PlanSetupFromJSONTyped(json, false)
}

export function PlanSetupFromJSONTyped(json: any, ignoreDiscriminator: boolean): PlanSetup {
  if (json == null) {
    return json
  }
  return {
    idx: json['idx'],
    direction: Vec3FromJSON(json['direction']),
    actions: (json['actions'] as Array<any>).map(PlanActionFromJSON),
    unmachined: json['unmachined'],
  }
}

export function PlanSetupToJSON(json: any): PlanSetup {
  return PlanSetupToJSONTyped(json, false)
}

export function PlanSetupToJSONTyped(
  value?: PlanSetup | null,
  ignoreDiscriminator: boolean = false,
): any {
  if (value == null) {
    return value
  }

  return {
    idx: value['idx'],
    direction: Vec3ToJSON(value['direction']),
    actions: (value['actions'] as Array<any>).map(PlanActionToJSON),
    unmachined: value['unmachined'],
  }
}
