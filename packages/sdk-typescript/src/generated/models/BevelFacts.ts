/* tslint:disable */
/* eslint-disable */
/**
 * Toolpath Engine API
 * Public REST API for Toolpath engine operations.  **Units.** All dimensional values — in parts and in feature details — are in millimetres; all angles are in degrees. Each part response also states this in its `units` field.  **Browser use.** CORS is per API key: a request only receives an `Access-Control-Allow-Origin` header when its `Origin` is in the allowlist configured on the key used (set when creating or editing the key in the portal). A key with no allowed origins is server-to-server only. A Bearer API key placed in client-side JavaScript is readable by anyone — restrict its origins, prefer a key granted read access only (writes made with it are rejected with 403 `read_only_key`), or proxy requests through your own server.  **Access and billing.** An API key is granted read and write access separately in each area of the API — the core API (uploads, reads, holders, and jobs), DFM, and Quoting — and that access can be edited on an existing key in the portal. Every operation states what it is billed as in its `x-toolpath-billing` field: `dfm` or `quoting` for a metered operation, which records usage, or `unmetered` for a core operation. `GET` reads; every other method writes. A key with no access to the operation’s area is refused with 403 `product_mismatch`; one with read access used for a write is refused with 403 `read_only_key`.
 *
 * The version of the OpenAPI document: 1.11.0
 *
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */

import { mapValues } from '../runtime.js'
import type { SinkFacts } from './SinkFacts.js'
import {
  SinkFactsFromJSON,
  SinkFactsFromJSONTyped,
  SinkFactsToJSON,
  SinkFactsToJSONTyped,
} from './SinkFacts.js'

/**
 * The bevel itself: how it leans, how far it runs, and what its surroundings allow a
 * cone to overshoot.
 * @export
 * @interface BevelFacts
 */
export interface BevelFacts {
  /**
   * Between the bevel's surface and the tool axis, degrees: zero for a wall, a right
   * angle for a floor. A matching tool's cone half angle is this.
   * @type {number}
   * @memberof BevelFacts
   */
  angleDeg: number
  /**
   * How far the bevel runs along its own slope — the length of cutting edge a tool
   * needs to span it in one pass.
   * @type {number}
   * @memberof BevelFacts
   */
  slant: number
  /**
   * The highest bottom among the features the bevel stands on — how far down there
   * is room under it before a tool would meet something. z grows up the tool axis,
   * so this is *below* the bevel's own `zMin` by the room there is.
   * @type {number}
   * @memberof BevelFacts
   */
  lowerAdjacentZMin: number
  /**
   * The bevel breaks the edge along an open pocket's floor.
   * @type {boolean}
   * @memberof BevelFacts
   */
  isOpenPocketBottom: boolean
  /**
   * Present only when the bevel is a countersink of revolution.
   * @type {SinkFacts}
   * @memberof BevelFacts
   */
  countersink?: SinkFacts
}

/**
 * Check if a given object implements the BevelFacts interface.
 */
export function instanceOfBevelFacts(value: object): value is BevelFacts {
  if (!('angleDeg' in value) || value['angleDeg'] === undefined) return false
  if (!('slant' in value) || value['slant'] === undefined) return false
  if (!('lowerAdjacentZMin' in value) || value['lowerAdjacentZMin'] === undefined) return false
  if (!('isOpenPocketBottom' in value) || value['isOpenPocketBottom'] === undefined) return false
  return true
}

export function BevelFactsFromJSON(json: any): BevelFacts {
  return BevelFactsFromJSONTyped(json, false)
}

export function BevelFactsFromJSONTyped(json: any, ignoreDiscriminator: boolean): BevelFacts {
  if (json == null) {
    return json
  }
  return {
    angleDeg: json['angleDeg'],
    slant: json['slant'],
    lowerAdjacentZMin: json['lowerAdjacentZMin'],
    isOpenPocketBottom: json['isOpenPocketBottom'],
    countersink: json['countersink'] == null ? undefined : SinkFactsFromJSON(json['countersink']),
  }
}

export function BevelFactsToJSON(json: any): BevelFacts {
  return BevelFactsToJSONTyped(json, false)
}

export function BevelFactsToJSONTyped(
  value?: BevelFacts | null,
  ignoreDiscriminator: boolean = false,
): any {
  if (value == null) {
    return value
  }

  return {
    angleDeg: value['angleDeg'],
    slant: value['slant'],
    lowerAdjacentZMin: value['lowerAdjacentZMin'],
    isOpenPocketBottom: value['isOpenPocketBottom'],
    countersink: SinkFactsToJSON(value['countersink']),
  }
}
