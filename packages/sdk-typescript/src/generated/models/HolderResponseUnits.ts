/* tslint:disable */
/* eslint-disable */
/**
 * Toolpath Engine API
 * Public REST API for Toolpath engine operations.  **Units.** All dimensional values — in parts and in feature details — are in millimetres; all angles are in degrees. Each part response also states this in its `units` field.  **Browser use.** CORS is per API key: a request only receives an `Access-Control-Allow-Origin` header when its `Origin` is in the allowlist configured on the key used (set when creating or editing the key in the portal). A key with no allowed origins is server-to-server only. A Bearer API key placed in client-side JavaScript is readable by anyone — restrict its origins, prefer a key granted read access only (writes made with it are rejected with 403 `read_only_key`), or proxy requests through your own server.  **Access and billing.** An API key is granted read and write access separately in each area of the API — the core API (uploads, reads, holders, and jobs), DFM, and Quoting — and that access can be edited on an existing key in the portal. Every operation states what it is billed as in its `x-toolpath-billing` field: `dfm` or `quoting` for a metered operation, which records usage, or `unmetered` for a core operation. `GET` reads; every other method writes. A key with no access to the operation’s area is refused with 403 `product_mismatch`; one with read access used for a write is refused with 403 `read_only_key`.
 *
 * The version of the OpenAPI document: 1.9.0
 *
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */

import { mapValues } from '../runtime.js'
/**
 * Units used by all dimensional values in this holder response.
 * @export
 * @interface HolderResponseUnits
 */
export interface HolderResponseUnits {
  /**
   * Unit used by every length and diameter measurement.
   * @type {HolderResponseUnitsLengthEnum}
   * @memberof HolderResponseUnits
   */
  length: HolderResponseUnitsLengthEnum
  /**
   * Unit used by every angular measurement.
   * @type {HolderResponseUnitsAngleEnum}
   * @memberof HolderResponseUnits
   */
  angle: HolderResponseUnitsAngleEnum
}

/**
 * @export
 */
export const HolderResponseUnitsLengthEnum = {
  Mm: 'mm',
} as const
export type HolderResponseUnitsLengthEnum =
  (typeof HolderResponseUnitsLengthEnum)[keyof typeof HolderResponseUnitsLengthEnum]

/**
 * @export
 */
export const HolderResponseUnitsAngleEnum = {
  Deg: 'deg',
} as const
export type HolderResponseUnitsAngleEnum =
  (typeof HolderResponseUnitsAngleEnum)[keyof typeof HolderResponseUnitsAngleEnum]

/**
 * Check if a given object implements the HolderResponseUnits interface.
 */
export function instanceOfHolderResponseUnits(value: object): value is HolderResponseUnits {
  if (!('length' in value) || value['length'] === undefined) return false
  if (value['length'] !== 'mm') return false

  if (!('angle' in value) || value['angle'] === undefined) return false
  if (value['angle'] !== 'deg') return false

  return true
}

export function HolderResponseUnitsFromJSON(json: any): HolderResponseUnits {
  return HolderResponseUnitsFromJSONTyped(json, false)
}

export function HolderResponseUnitsFromJSONTyped(
  json: any,
  ignoreDiscriminator: boolean,
): HolderResponseUnits {
  if (json == null) {
    return json
  }
  return {
    length: json['length'],
    angle: json['angle'],
  }
}

export function HolderResponseUnitsToJSON(json: any): HolderResponseUnits {
  return HolderResponseUnitsToJSONTyped(json, false)
}

export function HolderResponseUnitsToJSONTyped(
  value?: HolderResponseUnits | null,
  ignoreDiscriminator: boolean = false,
): any {
  if (value == null) {
    return value
  }

  return {
    length: value['length'],
    angle: value['angle'],
  }
}
