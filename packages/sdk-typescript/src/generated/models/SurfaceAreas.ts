/* tslint:disable */
/* eslint-disable */
/**
 * Toolpath Engine API
 * Public REST API for Toolpath engine operations.  **Early access.** Machining plans and toolpath calculation — creating, listing, and reading plans, calculating toolpaths, and reading toolpath results and machining time — are available now but still gaining functionality; planned additions include plan constraints and specifying material and stock, among others. Breaking changes still follow the API major version, so you can build against them today. These operations are grouped under the `Plans` and `Toolpaths` tags and marked `x-toolpath-experimental`.  **Units.** All dimensional values — in parts and in feature details — are in millimetres; all angles are in degrees. Each part response also states this in its `units` field.  **Browser use.** CORS is per API key: a request only receives an `Access-Control-Allow-Origin` header when its `Origin` is in the allowlist configured on the key used (set when creating or editing the key in the portal). A key with no allowed origins is server-to-server only. A Bearer API key placed in client-side JavaScript is readable by anyone — restrict its origins, prefer a key granted read access only (writes made with it are rejected with 403 `read_only_key`), or proxy requests through your own server.  **Access and billing.** An API key is granted read and write access separately in each area of the API — the core API (uploads, part display, holders, and jobs), DFM (part analysis, feature datasheets, and plans), and Quoting (toolpaths and machining time) — and that access can be edited on an existing key in the portal. Every operation states its area in its `x-toolpath-product` field (`core`, `dfm`, or `quoting`) and whether the call is billed in `x-toolpath-metered`. Reads are free but still belong to their product: reading a plan needs DFM access, and reading toolpaths needs Quoting. `GET` reads; every other method writes. A key with no access to the operation’s area is refused with 403 `product_mismatch`; one with read access used for a write is refused with 403 `read_only_key`.
 *
 * The version of the OpenAPI document: 1.12.1
 *
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */

import { mapValues } from '../runtime.js'
/**
 * What a feature's surface is made of: every region of it in exactly one bucket, so the five
 * add up to its surface area and nothing is counted twice.
 *
 * A description of the feature, not of a pass — see `projectedFloorArea` for the pair that
 * says what a pass sweeps, and the picture there for how the two families differ. A bucket is
 * zero where the feature has no such surface. Areas are of the meshed regions, so a curved
 * surface reads a shade under its exact area.
 * @export
 * @interface SurfaceAreas
 */
export interface SurfaceAreas {
  /**
   * Surface square to the tool axis, in mm².
   * @type {number}
   * @memberof SurfaceAreas
   */
  floorArea: number
  /**
   * Surface running along it, in mm².
   * @type {number}
   * @memberof SurfaceAreas
   */
  wallArea: number
  /**
   * The concave rounds between the two, in mm² — as surface, so a quarter round of radius
   * `r` over a length `L` reads `(π/2)·r·L`, not the `r·L` either projection sees.
   * @type {number}
   * @memberof SurfaceAreas
   */
  filletArea: number
  /**
   * The bevels, in mm².
   * @type {number}
   * @memberof SurfaceAreas
   */
  chamferArea: number
  /**
   * Everything else, in mm²: surfaces with no flat to stand on.
   * @type {number}
   * @memberof SurfaceAreas
   */
  contourArea: number
}

/**
 * Check if a given object implements the SurfaceAreas interface.
 */
export function instanceOfSurfaceAreas(value: object): value is SurfaceAreas {
  if (!('floorArea' in value) || value['floorArea'] === undefined) return false
  if (!('wallArea' in value) || value['wallArea'] === undefined) return false
  if (!('filletArea' in value) || value['filletArea'] === undefined) return false
  if (!('chamferArea' in value) || value['chamferArea'] === undefined) return false
  if (!('contourArea' in value) || value['contourArea'] === undefined) return false
  return true
}

export function SurfaceAreasFromJSON(json: any): SurfaceAreas {
  return SurfaceAreasFromJSONTyped(json, false)
}

export function SurfaceAreasFromJSONTyped(json: any, ignoreDiscriminator: boolean): SurfaceAreas {
  if (json == null) {
    return json
  }
  return {
    floorArea: json['floorArea'],
    wallArea: json['wallArea'],
    filletArea: json['filletArea'],
    chamferArea: json['chamferArea'],
    contourArea: json['contourArea'],
  }
}

export function SurfaceAreasToJSON(json: any): SurfaceAreas {
  return SurfaceAreasToJSONTyped(json, false)
}

export function SurfaceAreasToJSONTyped(
  value?: SurfaceAreas | null,
  ignoreDiscriminator: boolean = false,
): any {
  if (value == null) {
    return value
  }

  return {
    floorArea: value['floorArea'],
    wallArea: value['wallArea'],
    filletArea: value['filletArea'],
    chamferArea: value['chamferArea'],
    contourArea: value['contourArea'],
  }
}
