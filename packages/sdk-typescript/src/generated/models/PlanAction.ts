/* tslint:disable */
/* eslint-disable */
/**
 * Toolpath Engine API
 * Public REST API for Toolpath engine operations.  **Early access.** Machining plans and toolpath calculation — creating, listing, and reading plans, calculating toolpaths, and reading toolpath results and machining time — are available now but still gaining functionality; planned additions include plan constraints and specifying material and stock, among others. Breaking changes still follow the API major version, so you can build against them today. These operations are grouped under the `Plans` and `Toolpaths` tags and marked `x-toolpath-experimental`.  **Units.** All dimensional values — in parts and in feature details — are in millimetres; all angles are in degrees. Each part response also states this in its `units` field.  **Browser use.** CORS is per API key: a request only receives an `Access-Control-Allow-Origin` header when its `Origin` is in the allowlist configured on the key used (set when creating or editing the key in the portal). A key with no allowed origins is server-to-server only. A Bearer API key placed in client-side JavaScript is readable by anyone — restrict its origins, prefer a key granted read access only (writes made with it are rejected with 403 `read_only_key`), or proxy requests through your own server.  **Access and billing.** An API key is granted read and write access separately in each area of the API — the core API (uploads, part display, holders, and jobs), DFM (part analysis, feature datasheets, and plans), and Quoting (toolpaths and machining time) — and that access can be edited on an existing key in the portal. Every operation states its area in its `x-toolpath-product` field (`core`, `dfm`, or `quoting`) and whether the call is billed in `x-toolpath-metered`. Reads are free but still belong to their product: reading a plan needs DFM access, and reading toolpaths needs Quoting. `GET` reads; every other method writes. A key with no access to the operation’s area is refused with 403 `product_mismatch`; one with read access used for a write is refused with 403 `read_only_key`.
 *
 * The version of the OpenAPI document: 1.12.1
 *
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */

import { mapValues } from '../runtime.js'
/**
 *
 * @export
 * @interface PlanAction
 */
export interface PlanAction {
  /**
   * Identifier of this action record.
   * @type {string}
   * @memberof PlanAction
   */
  actionId: string
  /**
   * Position of this item in machining order.
   * @type {number}
   * @memberof PlanAction
   */
  idx: number
  /**
   * The strategy this action realizes, e.g. WholePartRoughing.
   * @type {string}
   * @memberof PlanAction
   */
  planCase: string
  /**
   * Name of the tool this action uses.
   * @type {string}
   * @memberof PlanAction
   */
  toolName: string
  /**
   * Feature tags (hex) this action machines — join to a part feature’s `featureTag` (from `GET /parts/{id}`). Empty for whole-part roughing and facing, which machine no named feature.
   * @type {Array<string>}
   * @memberof PlanAction
   */
  featureTags: Array<string>
  /**
   * The full synthesized tool spec, or null when the kernel exposes none. Its `key` names the tool within this plan only: two actions with equal keys run in one tool, so a change of key between consecutive actions is a tool change. It is not an identity across plans or kernel versions, and is absent on plans computed before tp-kernel 0.10.0.
   * @type {{ [key: string]: any | null; }}
   * @memberof PlanAction
   */
  tool: { [key: string]: any | null }
}

/**
 * Check if a given object implements the PlanAction interface.
 */
export function instanceOfPlanAction(value: object): value is PlanAction {
  if (!('actionId' in value) || value['actionId'] === undefined) return false
  if (!('idx' in value) || value['idx'] === undefined) return false
  if (!('planCase' in value) || value['planCase'] === undefined) return false
  if (!('toolName' in value) || value['toolName'] === undefined) return false
  if (!('featureTags' in value) || value['featureTags'] === undefined) return false
  if (!('tool' in value) || value['tool'] === undefined) return false
  return true
}

export function PlanActionFromJSON(json: any): PlanAction {
  return PlanActionFromJSONTyped(json, false)
}

export function PlanActionFromJSONTyped(json: any, ignoreDiscriminator: boolean): PlanAction {
  if (json == null) {
    return json
  }
  return {
    actionId: json['actionId'],
    idx: json['idx'],
    planCase: json['planCase'],
    toolName: json['toolName'],
    featureTags: json['featureTags'],
    tool: json['tool'],
  }
}

export function PlanActionToJSON(json: any): PlanAction {
  return PlanActionToJSONTyped(json, false)
}

export function PlanActionToJSONTyped(
  value?: PlanAction | null,
  ignoreDiscriminator: boolean = false,
): any {
  if (value == null) {
    return value
  }

  return {
    actionId: value['actionId'],
    idx: value['idx'],
    planCase: value['planCase'],
    toolName: value['toolName'],
    featureTags: value['featureTags'],
    tool: value['tool'],
  }
}
