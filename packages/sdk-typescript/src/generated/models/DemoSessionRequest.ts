/* tslint:disable */
/* eslint-disable */
/**
 * Toolpath Engine API
 * Public REST API for Toolpath engine operations.  **Units.** All dimensional values — in parts and in feature details — are in millimetres; all angles are in degrees. Each part response also states this in its `units` field.  **Browser use.** CORS is per API key: a request only receives an `Access-Control-Allow-Origin` header when its `Origin` is in the allowlist configured on the key used (set when creating or editing the key in the portal). A key with no allowed origins is server-to-server only. A Bearer API key placed in client-side JavaScript is readable by anyone — restrict its origins, prefer a key granted read access only (writes made with it are rejected with 403 `read_only_key`), or proxy requests through your own server.  **Access and billing.** An API key is granted read and write access separately in each area of the API — the core API (uploads, reads, holders, and jobs), DFM, and Quoting — and that access can be edited on an existing key in the portal. Every operation states what it is billed as in its `x-toolpath-billing` field: `dfm` or `quoting` for a metered operation, which records usage, or `unmetered` for a core operation. `GET` reads; every other method writes. A key with no access to the operation’s area is refused with 403 `product_mismatch`; one with read access used for a write is refused with 403 `read_only_key`.
 *
 * The version of the OpenAPI document: 1.11.0
 *
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */

import { mapValues } from '../runtime.js'
/**
 *
 * @export
 * @interface DemoSessionRequest
 */
export interface DemoSessionRequest {
  /**
   * A stable id the caller keeps per install (a UUID). Sending it makes renewals land on the same organization, so uploads and usage stay together. Omit it for a fresh throwaway organization each time. Treat it as a secret: this endpoint is unauthenticated, so anyone who knows an `installId` can mint a key on that organization, read what it uploaded, and retire the key its real holder is using. Generate it randomly (a v4 UUID), store it privately, and never show or log it.
   * @type {string}
   * @memberof DemoSessionRequest
   */
  installId?: string
}

/**
 * Check if a given object implements the DemoSessionRequest interface.
 */
export function instanceOfDemoSessionRequest(value: object): value is DemoSessionRequest {
  return true
}

export function DemoSessionRequestFromJSON(json: any): DemoSessionRequest {
  return DemoSessionRequestFromJSONTyped(json, false)
}

export function DemoSessionRequestFromJSONTyped(
  json: any,
  ignoreDiscriminator: boolean,
): DemoSessionRequest {
  if (json == null) {
    return json
  }
  return {
    installId: json['installId'] == null ? undefined : json['installId'],
  }
}

export function DemoSessionRequestToJSON(json: any): DemoSessionRequest {
  return DemoSessionRequestToJSONTyped(json, false)
}

export function DemoSessionRequestToJSONTyped(
  value?: DemoSessionRequest | null,
  ignoreDiscriminator: boolean = false,
): any {
  if (value == null) {
    return value
  }

  return {
    installId: value['installId'],
  }
}
