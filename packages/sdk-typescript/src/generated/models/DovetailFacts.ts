/* tslint:disable */
/* eslint-disable */
/**
 * Toolpath Engine API
 * Public REST API for Toolpath engine operations.  **Units.** All dimensional values — in parts and in feature details — are in millimetres; all angles are in degrees. Each part response also states this in its `units` field.  **Browser use.** CORS is per API key: a request only receives an `Access-Control-Allow-Origin` header when its `Origin` is in the allowlist configured on the key used (set when creating or editing the key in the portal). A key with no allowed origins is server-to-server only. A Bearer API key placed in client-side JavaScript is readable by anyone — restrict its origins, prefer a key granted read access only (writes made with it are rejected with 403 `read_only_key`), or proxy requests through your own server.  **Access and billing.** An API key is granted read and write access separately in each area of the API — the core API (uploads, reads, holders, and jobs), DFM, and Quoting — and that access can be edited on an existing key in the portal. Every operation states what it is billed as in its `x-toolpath-billing` field: `dfm` or `quoting` for a metered operation, which records usage, or `unmetered` for a core operation. `GET` reads; every other method writes. A key with no access to the operation’s area is refused with 403 `product_mismatch`; one with read access used for a write is refused with 403 `read_only_key`.
 *
 * The version of the OpenAPI document: 1.8.0
 *
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */

import { mapValues } from '../runtime.js'
import type { CdData } from './CdData.js'
import { CdDataFromJSON, CdDataFromJSONTyped, CdDataToJSON, CdDataToJSONTyped } from './CdData.js'

/**
 * A dovetail groove: overhanging walls leaning back at one angle over a floor, so the
 * space widens with depth and a tool has to enter through an opening narrower than where
 * it cuts.
 *
 * That inversion is why this carries widths rather than a medial-axis measurement. All
 * three are clearance diameters read at their own heights, with `0.0` standing for a
 * measurement that failed — which is what `isInvalidGeometry` reports.
 * @export
 * @interface DovetailFacts
 */
export interface DovetailFacts {
  /**
   * Discriminator for this facts variant.
   * @type {DovetailFactsKindEnum}
   * @memberof DovetailFacts
   */
  kind: DovetailFactsKindEnum
  /**
   * Between the overhanging wall and the tool axis, degrees, positive however the
   * wall leans.
   * @type {number}
   * @memberof DovetailFacts
   */
  taperDeg: number
  /**
   * Radius of the blend between wall and floor; 0.0 for the sharp kind.
   * @type {number}
   * @memberof DovetailFacts
   */
  filletRadius: number
  /**
   * The widest clearance over the floor — the width the groove cuts at.
   * @type {number}
   * @memberof DovetailFacts
   */
  floorWidth: number
  /**
   * The clearance through the opening at the top of the groove.
   * @type {number}
   * @memberof DovetailFacts
   */
  topOpeningWidth: number
  /**
   * The clearance just above the floor, under the overhangs.
   * @type {number}
   * @memberof DovetailFacts
   */
  bottomOpeningWidth: number
  /**
   * The groove's inner clearances vary from place to place — it runs out somewhere —
   * rather than being the one constant width a closed internal dovetail has.
   * @type {boolean}
   * @memberof DovetailFacts
   */
  isExternal: boolean
  /**
   * Derived from the widths and the taper, not measured off an axis, so the three
   * regimes hold one answer.
   * @type {CdData}
   * @memberof DovetailFacts
   */
  cd: CdData
  /**
   * A width could not be measured at all, so `cd` is arithmetic over a zero and no
   * tool should be offered on its strength.
   * @type {boolean}
   * @memberof DovetailFacts
   */
  isInvalidGeometry: boolean
}

/**
 * @export
 */
export const DovetailFactsKindEnum = {
  Dovetail: 'Dovetail',
} as const
export type DovetailFactsKindEnum =
  (typeof DovetailFactsKindEnum)[keyof typeof DovetailFactsKindEnum]

/**
 * Check if a given object implements the DovetailFacts interface.
 */
export function instanceOfDovetailFacts(value: object): value is DovetailFacts {
  if (!('kind' in value) || value['kind'] === undefined) return false
  if (value['kind'] !== 'Dovetail') return false

  if (!('taperDeg' in value) || value['taperDeg'] === undefined) return false
  if (!('filletRadius' in value) || value['filletRadius'] === undefined) return false
  if (!('floorWidth' in value) || value['floorWidth'] === undefined) return false
  if (!('topOpeningWidth' in value) || value['topOpeningWidth'] === undefined) return false
  if (!('bottomOpeningWidth' in value) || value['bottomOpeningWidth'] === undefined) return false
  if (!('isExternal' in value) || value['isExternal'] === undefined) return false
  if (!('cd' in value) || value['cd'] === undefined) return false
  if (!('isInvalidGeometry' in value) || value['isInvalidGeometry'] === undefined) return false
  return true
}

export function DovetailFactsFromJSON(json: any): DovetailFacts {
  return DovetailFactsFromJSONTyped(json, false)
}

export function DovetailFactsFromJSONTyped(json: any, ignoreDiscriminator: boolean): DovetailFacts {
  if (json == null) {
    return json
  }
  return {
    kind: json['kind'],
    taperDeg: json['taperDeg'],
    filletRadius: json['filletRadius'],
    floorWidth: json['floorWidth'],
    topOpeningWidth: json['topOpeningWidth'],
    bottomOpeningWidth: json['bottomOpeningWidth'],
    isExternal: json['isExternal'],
    cd: CdDataFromJSON(json['cd']),
    isInvalidGeometry: json['isInvalidGeometry'],
  }
}

export function DovetailFactsToJSON(json: any): DovetailFacts {
  return DovetailFactsToJSONTyped(json, false)
}

export function DovetailFactsToJSONTyped(
  value?: DovetailFacts | null,
  ignoreDiscriminator: boolean = false,
): any {
  if (value == null) {
    return value
  }

  return {
    kind: value['kind'],
    taperDeg: value['taperDeg'],
    filletRadius: value['filletRadius'],
    floorWidth: value['floorWidth'],
    topOpeningWidth: value['topOpeningWidth'],
    bottomOpeningWidth: value['bottomOpeningWidth'],
    isExternal: value['isExternal'],
    cd: CdDataToJSON(value['cd']),
    isInvalidGeometry: value['isInvalidGeometry'],
  }
}
