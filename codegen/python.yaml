project_name_override: toolpath
package_name_override: toolpath
package_version_override: 0.1.0
# `GET /v1/parts/{id}/thumbnail` answers `image/png`, which the generator does not know; it is
# bytes like any other binary body, so generate it as one.
content_type_overrides:
  image/png: application/octet-stream
