---
'@toolpath/viewer': minor
---

Add a visible section-plane gizmo with direction-coloured handles, a two-way drag handle, and
physical cut measurement. A custom `sectionHandle` theme colour still overrides the direction
colour.
